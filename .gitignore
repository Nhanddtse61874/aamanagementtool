## .NET / MSBuild artifacts
bin/
obj/

## Visual Studio
*.user
*.suo
.vs/
*.rsuser

## CodeRush (DevExpress)
.cr/

## NuGet
*.nupkg
*.snupkg
packages/
project.lock.json
project.fragment.lock.json
artifacts/

## Node / Angular (src/timesheet-web)
node_modules/
dist/
.angular/
*.tsbuildinfo
npm-debug.log*

## Built Angular UI copied into the API for single-process local deploy (deploy-local.bat). Build output — never commit.
src/TimesheetApp.Api/wwwroot/

## OS
.DS_Store
Thumbs.db
.claude/settings.local.json

## agent worktrees
.worktrees/

# M11: the default DbPath/ConfigPath/KeyRingPath land here so a fresh clone just runs.
# It is a real database with real local data - never commit it.
src/TimesheetApp.Api/data/

