# A2 — Log Work (Timesheet) — TimesheetTab.xaml + TimesheetViewModel

**Section risk:** Deleting WPF now permanently loses three whole affordances that never made it to the web Log Work page — picking another user's individual timesheet, filtering the grid by month/year, and any team-wide read-only aggregate view — plus the holiday/over-8h visual cues and cross-session collapse memory, with **no oracle left to notice**, because none of it exists to compare against once the WPF app is gone.

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| Week nav (Prev/Next, "Week of dd/MM/yyyy") | COVERED | `TimesheetViewModel.cs:263-275` (NextWeekAsync/PreviousWeekAsync); `TimesheetTab.xaml:126-134` | `log-work.component.ts:278-280` (prevWeek/nextWeek); `log-work.component.html:17-19` | [VERIFIED] Web also adds a "This week" shortcut WPF lacks — additive, not a loss. |
| DatePicker "jump to any week" (snaps to Monday) | MISSING | `TimesheetViewModel.cs:95-106` (JumpDate/OnJumpDateChanged); `TimesheetTab.xaml:138-139` | — | [VERIFIED] No date-picker or jump control anywhere in `log-work.component.html`/`.ts`. Only Prev/Next/This-week stepping exists; there is no way to jump directly to an arbitrary week. |
| Auto-save status text "Saving…" / "✓ Saved" / "⚠ error" | PARTIAL | `TimesheetViewModel.cs:354-383` (SaveStatus/SaveStatusIsError); `TimesheetTab.xaml:141-159` | `log-work.component.ts:305-348` (saveCell/clearCell), errors routed to `ToastService` (`log-work.component.ts:370-394`) | [VERIFIED] Errors ARE surfaced (as a toast, not inline text), but there is no persistent "Saving…" / "✓ Saved" affirmative indicator anywhere in the component/html/scss. |
| Smart fill (button) | COVERED | `TimesheetTab.xaml:160-162`; `SmartInputPanelVm` | `log-work.component.ts:460-525` (openSmartFill/applySmartFill); `log-work.component.html:22-24`; `smart-fill.ts` | [VERIFIED] Present and merges results correctly (`grid-state.ts:168-183` mergeSmartFill). Per-mechanism parity of the fill algorithm not exhaustively diffed — only its presence and merge-not-replace semantics. |
| Collapse-all toggle + persistence across app restarts (Settings key `entry.collapseAll`) | PARTIAL | `TimesheetViewModel.cs:242-261` (LoadCollapsePreferenceAsync/ToggleCollapseAll via `ISettingsRepository`) | `log-work.component.ts:118,848-862` (`collapsed` signal, collapseAll()) | [VERIFIED] Toggle works and survives an in-session SignalR refresh (the signal isn't reset in `applyWeek()`), but there is no backing store (no `localStorage`/settings API call anywhere in `pages/log-work/` — grepped, zero hits) — a browser refresh or new session always starts fully expanded. |
| Week-total chip | COVERED | `TimesheetTab.xaml:110-118` (WeekTotal) | `log-work.component.html:9-12` (weekTotal()) | [VERIFIED] |
| Filter: User picker — "Whole team (read-only)" + pick ANY individual active user to view/edit their grid | MISSING | `TimesheetViewModel.cs:110-131,176-189` (EntryTarget/Targets/EffectiveUserId); `TimesheetTab.xaml:173-175` | — | [VERIFIED] `log-work.component.ts` always calls `this.api.getWeek(monday)` with no `allUsers` arg (self-view only) — grepped every call site. The API route itself documents the removal: `TimesheetEndpoints.cs:56-62` — "Deliberately does NOT accept a userId query param: unlike WPF's EntryTarget picker, the web API never lets one user view another named user's individual grid." No UI control exists for it either. This is a genuine capability loss, not just a UI omission. |
| Filter: Month 1-12 / Year (y-2..y+3) — narrows which backlog groups show; DEFAULT always shown | MISSING | `TimesheetViewModel.cs:115-124,168-172,290-293` (SelectedMonth/FilterMonthNumber/FilterYear + client-side `Where` filter); `TimesheetTab.xaml:176-179` | — | [VERIFIED] `TimeLogService.GetWeekGroupedAsync`/`BuildGroupsAsync` (`TimesheetApp.Core/Services/TimeLogService.cs:244-261,300-328`, CORE) apply no `PeriodMonth` filter server-side, and `log-work.component.ts` applies none client-side either (only reference to `PeriodMonth`/`periodMonth` in the whole `log-work/` dir is in `move-month.ts`, for computing the NEXT month, not for filtering). Net effect: web always shows every backlog group regardless of period — the filter-down behavior described in the inventory does not exist at all. |
| Badge amber "Read-only (whole team)" | MISSING | `TimesheetTab.xaml:180-185` | — | [VERIFIED] Direct consequence of the missing team-view toggle above — nothing to badge. |
| Grid columns `TASK \| MON dd/MM ... FRI \| TOTAL` | COVERED | `TimesheetTab.xaml:226-255` (header), `267-410` (rows) | `log-work.component.html:39-45` (head), `94-124` (rows) | [VERIFIED] |
| Section band / Backlog header: code · project, type pill, period-month text, assignee, collapse caret, group total | PARTIAL | `TimesheetTab.xaml:277-315` (incl. `PeriodMonth` text at line 299) | `log-work.component.html:47-61` | [VERIFIED] Code, project, type badge, assignee avatar+name, caret, and group total are all present and covered. The `PeriodMonth` text itself (e.g. "2026-07") is NOT rendered anywhere in `log-work.component.html` — grepped, zero matches for `periodMonth`/`PeriodMonth` in the template. Minor, but the ticket's assigned-month is invisible on the web grid. |
| Task row: drag handle, name, 5 day cells (borderless input, blur/Enter commit), row total | COVERED | `TimesheetTab.xaml:317-385` (`GridDayBox` style, `UpdateSourceTrigger=LostFocus`) | `log-work.component.html:98-123` (`(blur)`/`(keyup.enter)` -> commitCell) | [VERIFIED] |
| Inline "➕ Add task" (dialog) | COVERED | `TimesheetTab.xaml:400-403` + `TaskInputDialog` | `log-work.component.html:132`; `add-task-dialog.component.ts:1-40`; `log-work.component.ts:539-552` (onAddTaskConfirmed) | [VERIFIED] `add-task-dialog.component.ts:7` explicitly states it matches WPF's `TaskInputDialog`. |
| "Move to next month ▶" (hidden for DEFAULT) | COVERED | `TimesheetTab.xaml:393-399`; `TimesheetViewModel.cs:469-487` (MoveMonthAsync, DEFAULT guard) | `log-work.component.html:139-142`; `log-work.component.ts:602-630` (onMoveMonth); `move-month.ts` (canMoveMonth — DEFAULT guard) | [VERIFIED] Web even hardens this with a second read-before-write guard the WPF version didn't need (whole-record PUT semantics) — additive. |
| Trash zone pinned bottom, visible only when editable | COVERED | `TimesheetTab.xaml:258-265` (Visibility bound to `CanEdit`) | `log-work.component.html:179-182` (`#trash` dropzone) | [VERIFIED] Web has no read-only Log Work state at all (no team-view), so the trash is unconditionally present — functionally equivalent since the gating condition (team view) never arises on web. |
| Footer "DAY TOTALS" row + week total | COVERED | `TimesheetTab.xaml:190-224` | `log-work.component.html:184-192` | [VERIFIED] |
| Data source: week-grouped read (self / all-users) | CORE-SURVIVES | `TimesheetViewModel.cs:284-286` calling `ITimeLogService` | `TimeLogService.GetWeekGroupedAsync` / `GetWeekGroupedAllUsersAsync` — `TimesheetApp.Core/Services/TimeLogService.cs:244-292` | [VERIFIED] Both methods live in Core and are called by both WPF and (for the self-view half) the API (`TimesheetEndpoints.cs:63-83`). Deletion cannot lose the data-shaping logic itself — only the WPF-side UI affordances that never called `GetWeekGroupedAllUsersAsync` from the web layer (see the User-picker/team-badge rows above). |
| Scope: active-team-only tasks/backlogs | CORE-SURVIVES | Inventory cites `TimeLogService:162,212-213` (line numbers shifted; current logic at `TimesheetApp.Core/Services/TimeLogService.cs:220-221,304-305,334-336` via `ICurrentTeamService.ActiveTeamId`) | same file (Core) | [VERIFIED] |
| Client-side validation with inline red-border, blocks the write before any network call (`TimesheetRowVm`: >0, ≤8, ≤1 decimal) | PARTIAL | `TimesheetRowVm.cs:46-59` (`Validate`/`INotifyDataErrorInfo`); `TimesheetTab.xaml:347` (`ValidatesOnNotifyDataErrors=True`) | `grid-state.ts:197-202` (`parseHours` — parses only, no rule check); rejection handled after the fact by `log-work.component.ts:370-394` (toast on 400) | [VERIFIED] The RULES themselves are still enforced (see next row, CORE), but the specific "client blocks before writing, cell shows red" UX is gone: web always attempts the PUT and reverts + toasts on a 400. No inline invalid-cell styling exists (grepped `log-work.component.scss` — no error/invalid class on `.cell`). |
| Server-side validation (>0, ≤1 decimal, no Sat/Sun, no holiday, cell ≤8h, day total ≤8h) | CORE-SURVIVES | `TimesheetViewModel.SaveCellAsync` calls `ITimeLogService.SaveCellAsync` (`TimeLogService.cs:42-60`) | `TimesheetEndpoints.cs:84-117` (PUT `/api/timesheet/cell`) calls `ITimeLogService.SaveCellCheckedAsync` -> `ValidateCellAsync` (`TimeLogService.cs:77-88`, same rule set) | [VERIFIED] Both paths converge on Core; the business rules cannot be lost by deleting WPF. |
| Type → LostFocus/blur → auto-save (no Save button) | COVERED | `TimesheetViewModel.cs:345-350,369-383` (DayChanged -> AutoSaveCellAsync) | `log-work.component.ts:291-303` (commitCell on blur/Enter) | [VERIFIED] |
| Clear cell = DELETE (not save-of-zero) | COVERED | `TimesheetViewModel.SaveCellAsync` (empty -> `ClearCellAsync`, `TimesheetViewModel.cs:463`) | `log-work.component.ts:330-348` (clearCell -> DELETE `/api/timesheet/cell`) | [VERIFIED] |
| Drag reorder, same backlog group only | COVERED | `TimesheetViewModel.cs:409-423` (AreInSameGroup/ReorderTaskAsync) | `log-work.component.ts:696-719` (onDrop); `reorder.ts` (reorderPlan) | [VERIFIED] |
| Drag to trash = soft-delete (`is_active=0`, TimeLogs preserved) | COVERED | `TimesheetViewModel.cs:426-431` (DeleteTaskAsync -> `SetActiveAsync(id, false)`) | `log-work.component.ts:764-815` (onTrash arms, confirmDelete writes -> `setTaskActive(id, false)`) | [VERIFIED] Web adds a confirmation dialog WPF never had (WPF deletes on drop with no confirm) — additive safety, not a regression. |
| Team view = fully read-only (no trash/drag/Add; cell writes are a faked no-op Ok) | MISSING | `TimesheetViewModel.cs:130-132,371,446,452` (IsTeamView/IsReadOnly/CanEdit gates) | — | [VERIFIED] Direct consequence of the missing team-view toggle: since the Log Work page never requests `allUsers=true`, this whole read-only mode (and its specific "fake Ok" save behavior) never exists on web. |
| Holiday day-columns: distinct background, IsReadOnly, "Holiday" watermark, tooltip | MISSING | `TimesheetViewModel.cs:134-157` (per-day IsHoliday/ReadOnly flags); `TimesheetTab.xaml:27-36,46-66,342-376` (HolidayCellBorder/GridDayBox watermark) | — | [VERIFIED] Zero matches for "holiday" (case-insensitive) anywhere in `log-work.component.ts`, `.html`, or `.scss`. The underlying RULE still blocks a holiday save server-side (`TimeLogService.ValidateCellAsync` calls `_holidays.IsHolidayAsync`, CORE) — a save attempt gets a 400 — but the user gets no visual warning before typing, no read-only cell, no watermark, no tooltip. A user can type hours into a holiday cell and only find out it was rejected after the fact via a toast. |
| Footer day-total > 8h turns red + bold | MISSING | `TimesheetTab.xaml:8-17` (`TotalTextBlock` style + `OverEightTagConverter`); bound at `204-222` | `log-work.component.html:184-192` only has `[class.zero]` | [VERIFIED] Grepped `log-work.component.scss` for over/8h/danger/warn styling tied to day totals — the only danger styling found is for the trash dropzone during drag (`.dropzone.cdk-drop-list-receiving`, scss:97-98), unrelated to the footer. No >8h visual warning exists on the day-total footer. |

