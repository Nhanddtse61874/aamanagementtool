# A5 — Daily Report — DailyInputTab + DailyBoardTab

**Section risk:** The one behavior that WILL be lost outright is DR-09's "auto-backfill on startup" — the WPF desktop app is currently the ONLY thing that ever calls `BackfillMissingWeeksAsync`, and the API never does; deleting `src/TimesheetApp/` removes the last caller, so any week nobody manually archives via the (now admin-only) "Archive week" button silently never gets a markdown snapshot, forever, with no error and no oracle to notice.

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| Prev/Next day navigation | COVERED | `MainWindow.xaml:221,224` (PrevDayCommand/NextDayCommand) · `DailyReportViewModel.cs:94-98` | `daily-report.component.html:9,11` · `daily-report.component.ts:234-242` | [VERIFIED] |
| Direct date-jump via calendar picker | PARTIAL | `MainWindow.xaml:222` (`<DatePicker SelectedDate=.../>` — click any date) | `daily-report.component.html:10` (`{{ dayLabel() }}` — plain text, no calendar input anywhere in the file) | [VERIFIED] Only ◀/▶ remain on the web; reaching a date N days away now takes N clicks instead of one calendar pick. Every date is still *reachable*, so this is a usability narrowing, not a hard loss. |
| Archive week — writes markdown file | COVERED | `StandupArchiveService.cs` (Core, `ExportWeekAsync`/`BuildMarkdownAsync`) — CORE-SURVIVES, invoked by `DailyReportViewModel.cs:100-105` | `TimesheetApp.Api/Endpoints/AdminEndpoints.cs:133-154` (`POST /api/standup/archive`) · `daily-report.component.ts:492-497` | [VERIFIED] Same Core builder → byte-identical file format. |
| Archive week — WHO can trigger it | PARTIAL | `MainWindow.xaml:225-226` + `DailyReportViewModel.cs:100-105` — **no admin check**, any signed-in desktop user can click it | `AdminEndpoints.cs:150` `.RequireAuthorization(AuthSetup.AdminPolicy)` · `daily-report.component.ts:190-198,`html:18-22` — button only rendered for `isAdmin()` | [VERIFIED] Deliberate tightening (self-documented in both files as `[ADMIN]`), but it IS a permission narrowing versus WPF: a non-admin user who could archive the week from the desktop app can no longer do so from the web. |
| Auto-backfill missing weekly archives on startup | **MISSING** | `App.xaml.cs:99-101` — `IStandupArchiveService.BackfillMissingWeeksAsync()` called on every WPF startup (DR-09 acceptance: "a startup with an un-archived completed week produces its file") | none — confirmed absent. `AdminEndpoints.cs:16-21` and `AdminEndpointsTests.cs:299-301` both explicitly document: *"`Program.cs` never calls `BackfillMissingWeeksAsync` (only the WPF `App.xaml.cs` does), so on a web-only deployment nothing ever wrote it."* | [VERIFIED] Already a known, self-documented gap in the code today. Deleting WPF removes the ONLY caller that exists. No scheduled job/background service replaces it. This is the section's headline risk. |
| Input tab — Add entry button + modal (backlog/task picker, ad-hoc, description, deadline, status) | COVERED | `DailyInputTab.xaml:118-124` (button) + `StandupEntryDialog` + `StandupDraftVm` (`DailyReportViewModel.cs:288-349`) | `daily-report.component.html:34-39,217-291` · `daily-report.component.ts:253-319` | [VERIFIED] Hidden when locked in both. |
| Quick Import — clone a day's entries, appended | COVERED (mechanism) | `StandupService.cs:118-151` (Core) · `DailyInputTab.xaml:123-124` | `POST /api/standup/quick-import` (`SettingsEndpoints.cs:878-908`) · `daily-report.component.ts:344-357` | [VERIFIED] Same Core clone logic (new ids/order/timestamps, source untouched). |
| Quick Import — pick an ARBITRARY past source date | **PARTIAL** | `QuickImportDialog.xaml:26-30` + `.xaml.cs:13` — a free `DatePicker`, defaults to yesterday but any past day can be chosen | `daily-report.component.ts:344-346` — `const source = addDays(target, -1);` **hardcoded**, no date-picker UI at all for the source day | [VERIFIED] Real capability loss: WPF user can import from e.g. "3 days ago"; web user can only ever import from "the day immediately before the day on screen." |
| Entry card — code/task/status/description/deadline | COVERED | `DailyInputTab.xaml:27-38` | `daily-report.component.html:57-73` | [VERIFIED] |
| Delete entry (owner + editable only) | COVERED | `DailyInputTab.xaml:110-112` | `daily-report.component.html:126-131` | [VERIFIED] |
| Drag reorder within a section | COVERED | `DailyInputTab.xaml.cs:76-84` (`OnEntryDrop`) → `StandupService.ReorderEntryAsync` (Core) | `daily-report.component.ts:370-394` (`onDrop`) → `PUT /api/standup/entries/reorder` (`SettingsEndpoints.cs:841-875`) | [VERIFIED] |
| Drag onto other section = move section | COVERED | Same as above — `ReorderEntryAsync` sets `Section = target.Section` (`StandupService.cs:198`) | `cdkDropListGroup` (html:31) lets a card cross into the other section's droplist; same reorder endpoint | [VERIFIED] |
| Drag onto trash = delete | COVERED | `DailyInputTab.xaml:128-135` + `.xaml.cs:100-107` | `daily-report.component.html:139-149` · `.ts:377-380` | [VERIFIED] |
| Edit-lock rule (today/yesterday only) | CORE-SURVIVES | `StandupService.cs:34-38` | `SettingsEndpoints.cs` (every write route re-checks) + mirrored client-side in `standup-day.ts:69-71` for UI-offer purposes only | [VERIFIED] Business rule lives in Core; deletion of WPF cannot lose it, and the API is confirmed to actually call it. |
| Owner-only entry edit/delete gate | CORE-SURVIVES | `StandupService.cs:158,180,194` | Enforced identically via `IStandupService` calls in `SettingsEndpoints.cs` (e.g. 801,827) | [VERIFIED] |
| Issues exempt from lock/owner gate (collaborative, DR-04) | CORE-SURVIVES | `StandupService.cs:7,222-250` (no `CanEditDay`/owner check in Add/Update/DeleteIssueAsync) | `SettingsEndpoints.cs:910-912` (explicit comment) · `daily-report.component.ts:58-63` (explicit comment) | [VERIFIED] Design intent documented identically on both sides. |
| Issue add (+ Issue, text required) | COVERED | `DailyInputTab.xaml:44-45` + `StandupIssueDialog` | `daily-report.component.html:77-93` · `.ts:421-436` | [VERIFIED] |
| Issue solution add/edit inline + status | COVERED | `DailyInputTab.xaml:76-93` (`ShowSolutionEditor`/`SaveCommand`) | `daily-report.component.html:104-115` · `.ts:448-473` | [VERIFIED] Web additionally guards with optimistic-concurrency (`UpdateIssueCheckedAsync`, 409 handling) — a strict improvement, not a loss. |
| Issue delete | COVERED | `DailyInputTab.xaml:59-60` (`DeleteCommand`) | `daily-report.component.html:100-101` · `.ts:475-482` | [VERIFIED] |
| Issue color coding: amber (no solution) / green (has solution) | COVERED | `DailyReportViewModel.cs:265` (`HasSolution => !IsNullOrWhiteSpace(SolutionText)`) + `DailyInputTab.xaml:97-105`,`DailyBoardTab.xaml:51-59` (DataTrigger) | `standup-entry.ts:116-122` (`hasSolution`, comment explicitly cites the exact WPF trigger) + `daily-report.component.scss:36-37` (`.issue.ok`/`.issue.warn`) | [VERIFIED] Trigger logic matches exactly — NOT `Status == "resolved"`, confirmed on both sides. |
| Board tab — multi-team checkbox filter | COVERED | `DailyBoardTab.xaml:99-102` (`ctrl:TeamFilter`) + `TeamFilterViewModel` | `daily-report.component.html:154` (`<app-team-filter>`) · `.ts:244-247` | [VERIFIED] |
| Board tab — card = MEMBER of checked teams (union), incl. empty cards for non-reporters | CORE-SURVIVES | `StandupService.cs:57-86` (`GetTeamStandupAsync` — builds from `_teams.GetUserIdsForTeamAsync`, not "all active users") | `GET /api/standup/board` (`SettingsEndpoints.cs:743-758`) calls the same Core method; `daily-report.component.html:166-211` renders whatever the server returns, including empty-entry cards | [VERIFIED] |
| Board tab — teamIds empty/0 ⇒ board empty (no all-teams leak) | COVERED | `StandupService.cs:59-61` (Core: `effectiveTeams.Count==0 → empty`) | Defended TWICE on the web: server (same Core rule) AND client (`daily-report.component.ts:149,212-217` `boardBlocked` — refuses to even call the API on `[]`, since an absent key means "all teams" to the server) | [VERIFIED] |
| Board tab — entry display (code/task/due/status/description) | COVERED | `DailyBoardTab.xaml:20-30` | `daily-report.component.html:182-195` | [VERIFIED] |
| Board tab — issue display shows the SOLUTION TEXT for resolved issues | **MISSING** | `DailyBoardTab.xaml:45-48` — `Text="{Binding SolutionText}"` shown (read-only) whenever `HasSolution` is true, so any team member reading the board can see how a colleague's issue was solved | `daily-report.component.html:196-200` — renders **only** `i.issueText`; `i.solutionText` is never bound anywhere in the board branch of the template | [VERIFIED] Read the full 215-line template — confirmed no other rendering path. A board viewer can now only see THAT an issue was solved (✓), never HOW, unless they are that entry's own owner viewing their own Input tab. |

## Enumeration note
26 distinct behaviors audited (toolbar nav ×4, input-tab CRUD/DnD ×7, edit-lock/owner/issue-exemption rules ×3, issue CRUD/coloring ×4, board tab ×5, archive/backfill ×3). 3 are CORE-SURVIVES (pure Core business rules, confirmably wired into the API), 17 are COVERED [VERIFIED] with citations both sides, and **5 are PARTIAL/MISSING** — see rows above. Zero verdicts were guessed; every COVERED row has a web-side citation actually read, not assumed.
