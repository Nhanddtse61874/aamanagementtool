# B3 — Multi-team + the R6 anti-leak rule

**Section risk:** Almost the entire scoping engine (R6 SQL rule, active-team resolution algorithm, bootstrap/backfill, DEFAULT-per-team creation) already lives in `TimesheetApp.Core` and/or has a dedicated `TimesheetApp.Api` implementation, so WPF deletion cannot lose the *rules*; the one real risk is UI-surface, not logic — the web app has **no working active-team switcher** (sidebar `<select>` is static mockup markup) and the **Backlog page has no team-filter control at all** (unlike its WPF counterpart), so two of the nine screen/behavior combinations described in the inventory are not actually reachable by a web user today, even though their backend routes exist.

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `AvailableTeams = UserTeams(user) ∩ Teams(is_active=1)` formula | COVERED [VERIFIED] | `src/TimesheetApp/Services/CurrentTeamService.cs:107-113` (`ResolveAvailableAsync`) | `src/TimesheetApp.Api/Infrastructure/ApiCurrentTeamService.cs:61-64` | API has its own scoped implementation of `ICurrentTeamService` (Core interface) reproducing the exact same intersection; comment at ApiCurrentTeamService.cs:18 states it "mirrors the WPF service exactly". |
| `ActiveTeamId` resolution (persisted-if-still-valid : first-available : 0) | COVERED [VERIFIED] | `CurrentTeamService.cs:58-63` | `ApiCurrentTeamService.cs:66-69` | Same three-way fallback, byte-for-byte identical logic, independently re-implemented for the per-request API host. |
| `ActiveTeamId` persistence location | CORE-SURVIVES [VERIFIED] | *(inventory text is stale)* `CurrentTeamService.cs:81` calls `_users.SetActiveTeamIdAsync` | `TimesheetApp.Core/Data/Repositories/UserRepository.cs:170-183`, `IUserRepository.cs:52-53` | The inventory's "%APPDATA%, intentionally not DB" description is **outdated**: M8.2/Wave 4 already moved this off `IAppConfig` into `Users.active_team_id` (per-user DB column) — see `IAppConfig.cs:24-25` and `JsonAppConfig.cs:12-17` (legacy key now ignored). Both WPF and API read/write the same Core repository method today, so the anti-leak intent the inventory describes is already satisfied at the Core/DB layer, independent of WPF. |
| Live re-resolve on `DataKind.Teams` broadcast + `_suppressReentry` feedback-loop guard | COVERED [VERIFIED] | `CurrentTeamService.cs:38-42, 93-104` | `src/timesheet-web/src/app/components/team-filter/team-filter.component.ts:118-135` (subscribes to `DataKind.Teams`, calls `reload()`); `SettingsEndpoints.cs:97-137` (`PUT /api/me/active-team` deliberately never broadcasts `DataKind.Teams` to the switching user) | Different technique, same outcome: WPF broadcasts-then-suppresses-self; the API simply never broadcasts a per-user active-team switch at all (comment: "the one mutating route in the API that notifies nobody, deliberately"). Both prevent a self-triggered reload loop. |
| Guard: do NOT raise `ActiveTeamChanged` inside `InitializeAsync` (WPF `Measure` stack-overflow workaround) | COVERED [VERIFIED — N/A by architecture] | `CurrentTeamService.cs:65-69`; consumed by `MainViewModel.cs:238-239` (`RefreshTeamsFromService()` called explicitly after `InitializeAsync`) | `team-filter.component.ts:103-116` (`ngOnInit`, not constructor — "THE SEED RUNS HERE, NOT IN THE CONSTRUCTOR, AND THAT IS LOAD-BEARING") | This is a defensive workaround for a WPF-specific bug (synchronous `Measure`/layout re-entrancy), not a business rule — Angular's change-detection model has no analogous reentrancy hazard. Angular has its own, differently-motivated ordering safeguard in the same file, so no behavior is actually lost. |
| **R6 anti-leak rule**: `teamIds == null` ⇒ no filter (all teams); `teamIds` empty or `teamId == 0` ⇒ match nothing, never "all" | CORE-SURVIVES [VERIFIED] | *(rule is enforced in Core, not WPF-specific)* | `TimesheetApp.Core/Data/Repositories/BacklogRepository.cs:25-34`, `StandupRepository.cs:33-67`, `TaskRepository.cs:51-61`, `TimeLogRepository.cs:86-128` — all `WHERE (@noTeam OR team_id IN @teamIds)` | The rule lives entirely in Core repository SQL, shared verbatim by WPF and the API. The API additionally **hardens** it: `BacklogEndpoints.cs:36`, `TimesheetEndpoints.cs:301-406` intersect client-supplied `teamIds` with `ctx.MemberTeamIds` server-side (WPF, as a trusted single-user desktop client with direct DB access, does not need this extra step). Nothing is lost; the web side is strictly safer. |
| Screen scope — Log Work / Daily Input: **Active team ONLY** | CORE-SURVIVES [VERIFIED] | *(enforced in Core services, not WPF UI code)* — WPF ViewModel evidence: `TimesheetViewModel.cs:63` (Smart Fill scoped to `_currentTeam.ActiveTeamId`), `DailyReportViewModel.cs` uses `StandupService` | `TimesheetApp.Core/Services/TimeLogService.cs:221,305,334,353` (`GetActiveForTimesheetAsync(_currentTeam.ActiveTeamId)`); `TimesheetApp.Core/Services/StandupService.cs:46,55,89-90,107,124,200,273` (every read/write scoped to `_currentTeam.ActiveTeamId`); web-side confirmation: `TimesheetApp.Api/Endpoints/TimesheetEndpoints.cs:56-62` (week grid, no client teamIds accepted) and `src/timesheet-web/src/app/pages/daily-report/standup-entry.ts:124-141` (backlog picker filtered to `activeTeamId`, tracked as DR-11) | Both the Log Work grid and the Daily-Input backlog picker are scoped to the active team by the *same Core service code* WPF calls — this is not duplicated logic, it is the identical method. |
| Screen scope — **Backlog**: TeamFilter (multi) | PARTIAL [VERIFIED] | `src/TimesheetApp/ViewModels/RequestsViewModel.cs:11-49` (class `BacklogsViewModel`; despite the file name, this is the WPF Backlog screen's VM) — `TeamFilter = new TeamFilterViewModel(_currentTeam)` at line 43, `SearchAsync(null, teamIds)` at line 92 | `src/timesheet-web/src/app/pages/backlog/backlog.component.ts:108` calls `this.api.getBacklogList()` with **no team parameter**; `worklog.service.ts:463-465` (`getBacklogList()` takes zero args, comment: "Returns EVERY backlog"); generated client `src/timesheet-web/src/app/api/fn/backlogs/backlog-list.ts:12-14` (`BacklogList$Params` only has `term`, no `teamIds`) | The **route** supports team scoping server-side (`BacklogEndpoints.cs:44-57`, `EffectiveTeamIds(http, ctx)`, `SearchAsync(term, effectiveTeamIds)`), but that capability is not exposed through the OpenAPI-generated client and the Backlog page has **no `<app-team-filter>`** anywhere in `backlog.component.ts`/`.html` (confirmed by grep — zero "team" hits in the template). A web user cannot narrow the Backlog list to a subset of their teams the way WPF's Backlog screen lets them; today it always shows every backlog across every team they belong to. Route exists, UI does not — PARTIAL, leaning toward functionally unreachable. |
| Screen scope — **Task List**: TeamFilter (multi) | COVERED [VERIFIED] | `src/TimesheetApp/ViewModels/TaskListViewModel.cs:66-67,182-183,208` (`TeamFilter = new TeamFilterViewModel(_currentTeam)`, `SearchAsync(null, teamIds)`, `ShowTeamColumn`) | `src/timesheet-web/src/app/pages/task-list/task-list.component.html:29` (`<app-team-filter (selectionChange)="onTeams($event)" />`) plus the empty-state handling at lines 52-59 | Full port, including the "empty selection renders locally, no API call" contract. |
| Screen scope — **Daily Board**: TeamFilter (multi) | COVERED [VERIFIED] | `src/TimesheetApp/ViewModels/DailyReportViewModel.cs:20,38-40` (`TeamFilter = new TeamFilterViewModel(_currentTeam)`) | `src/timesheet-web/src/app/pages/daily-report/daily-report.component.ts:146,149,216,245` (`teamIds` signal wired into `getStandupBoard(day, teamIds())`, `boardBlocked` on empty selection) | Matches WPF's "board reloads on selection change" contract. |
| Screen scope — **Reports / Export**: TeamFilter (multi) | COVERED [VERIFIED] | `src/TimesheetApp/ViewModels/ReportsViewModel.cs` (`TeamFilter` field, confirmed present via grep) | `src/timesheet-web/src/app/pages/reports/reports.component.html:49` (`<app-team-filter (selectionChange)="onTeamSelectionChange($event)"/>`), export button gated on `teamEmpty()` at line 52-55, drill-down labelled "Team → Project → Backlog → Task → Date" at line 114 | Reports and Export share one screen/one TeamFilter instance in both WPF and web, exactly as the inventory groups them. |
| **`TeamBootstrapService`** — one-time startup: idempotent re-run backfill on existing team; "Architect Improvement" if business data present, else "My Team" | CORE-SURVIVES [VERIFIED] | *(same class used by both — not WPF-specific)* | `TimesheetApp.Core/Services/TeamBootstrapService.cs:44-63` (`EnsureBootstrappedAsync`); registered and invoked at API startup: `TimesheetApp.Api/Program.cs:116,178` | This is not a port — it is the literal same `TimesheetApp.Core.Services.TeamBootstrapService` class, already wired into the API's own startup pipeline. Nothing to lose. |
| Backfill transaction (`Backlogs.team_id`, `StandupEntries.team_id`, `UserTeams` insert, `Users.active_team_id` seed) | CORE-SURVIVES [VERIFIED] | *(same file)* | `TeamBootstrapService.cs:85-128` (`BackfillTeamAsync`) | Same file/method as above; runs identically regardless of which host (WPF or API) triggers `EnsureBootstrappedAsync` at startup. |
| **DEFAULT backlog — 1 per team (TM-04)**; new team auto-creates DEFAULT backlog + syncs default tasks | COVERED [VERIFIED] | `src/TimesheetApp/ViewModels/SettingsViewModel.cs:560-569` (`AddTeamAsync`: `_teams.InsertAsync` → `_sync.EnsureDefaultBacklogIdAsync(newId)` → `_sync.SyncAsync()`) | `TimesheetApp.Api/Endpoints/SettingsEndpoints.cs:226-258` (`POST /api/teams`: identical `teams.InsertAsync` → `sync.EnsureDefaultBacklogIdAsync(id)` → `sync.SyncAsync()`, comment: "MIRRORS SettingsViewModel.AddTeamAsync"); UI: `src/timesheet-web/src/app/pages/settings/settings.component.ts:424-432` (`addTeam()`, toast: "Team created — with its DEFAULT backlog") | Full round-trip verified: Core sync service (`IDefaultTaskSyncService`), API route, and a real Angular Settings → Teams tab UI (create/rename/activate/membership) all present and wired, confirmed by reading `settings.component.ts:403-500`. |

## Additional finding not itemized in the inventory text but directly implied by it

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| User-facing **active-team switcher** (the write side of `ActiveTeamId` — actually changing your active team from the UI) | PARTIAL [VERIFIED] | WPF sidebar switcher calls `ICurrentTeamService.SetActiveTeamAsync` (interface: `ICurrentTeamService.cs:27`) | Backend fully wired: `SettingsEndpoints.cs:88-141` (`PUT /api/me/active-team`), generated client `src/timesheet-web/src/app/api/fn/me/me-set-active-team.ts:32`, `WorklogService.setActiveTeam()` at `worklog.service.ts:1225-1239`. UI: **`src/timesheet-web/src/app/components/sidebar/sidebar.component.html:44-52`** — the "TEAM" selector is static, hard-coded markup (`<option>Architect Improvement</option><option>Plus Team</option>`), not bound to any component signal or click handler. | Explicitly documented as a known gap in the web code itself: `team-filter.component.ts:142` — "the web app has no team switcher yet: `WorklogService.setActiveTeam()` exists and has no caller... the sidebar's team `<select>` is still hard-coded mockup markup." A web user today cannot change their active team at all. This is the single largest hole in this section: everything else (R6, scoping, bootstrap) is proven safe, but the one interactive control that lets a multi-team user actually USE that machinery is unbuilt. |
