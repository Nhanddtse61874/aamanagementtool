# B4 — Daily Report / Standup business rules

**Section risk:** None — every rule enumerated below is implemented and enforced inside `TimesheetApp.Core` (models, `StandupService`, `StandupRepository`, `DatabaseInitializer`); the WPF `DailyReportViewModel` only calls into `IStandupService` (single call site: `CanEditDay`, line 72) and reimplements none of it, so deleting `src/TimesheetApp/` cannot lose any of this behavior.

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `StandupEntry` schema (Id, UserId, WorkDate, Section, BacklogId, BacklogCode, TaskText, Description, Deadline, Status, OrderIndex, CreatedAt, TeamId) | CORE-SURVIVES | n/a — WPF only binds to it, does not declare it | `TimesheetApp.Core/Models/StandupModels.cs:9-13` | Record type lives in Core; WPF has no parallel copy. |
| `StandupIssue` schema (Id, EntryId, IssueText, SolutionText, Status, OrderIndex, CreatedAt, RowVersion) | CORE-SURVIVES | n/a | `TimesheetApp.Core/Models/StandupModels.cs:20-26` | Includes `HasSolution` derived flag and M8.2 `RowVersion` optimistic-concurrency column. |
| Entry `Status` enum = `Todo \| In-process \| Done \| Pending` | CORE-SURVIVES | n/a | `TimesheetApp.Core/Models/StandupModels.cs:29-33` (`StandupStatus.All`); validated in `StandupService.cs:257` (`ValidateDraft`) | Also re-exposed to Angular via `settings-standup-entry-*` DTOs, but the source of truth is Core. |
| Issue `Status` enum = `open \| pending \| resolved` | CORE-SURVIVES | n/a | `TimesheetApp.Core/Models/StandupModels.cs:36-39` (`StandupIssueStatus.All`); validated in `StandupService.cs:225,233,244` | |
| `BacklogId` nullable, ad-hoc backlog code typed in meeting (DR-03), resolved via `ResolveBacklogIdAsync` (not-found → `null`) | CORE-SURVIVES | `Views/Dialogs/StandupEntryDialog.xaml.cs` only calls the service, does not implement resolution | `TimesheetApp.Core/Services/StandupService.cs:263` (`ResolveBacklogIdAsync`), called from `AddEntryAsync` (line 102) and `UpdateEntryAsync` (line 161) | Pure Core logic; WPF dialog is just a form. |
| Issue **CASCADE DELETE** when parent entry deleted | CORE-SURVIVES | n/a | `TimesheetApp.Core/Data/DatabaseInitializer.cs:126` (`FOREIGN KEY (entry_id) REFERENCES StandupEntries(id) ON DELETE CASCADE`), confirmed by comment in `StandupRepository.cs:132` | Enforced at the SQLite schema level, not in app code — cannot be lost by deleting either UI layer. |
| `OrderIndex` scoped per (user, day, section, **team**) | CORE-SURVIVES | n/a | `TimesheetApp.Core/Services/StandupService.cs:199-218` (`ReorderEntryAsync` — "Scope order math to the active team so reordering doesn't churn other teams' OrderIndex (I4)") | Drag-reorder math lives entirely in `StandupService`. |
| **Edit-lock**: entries editable only for today + yesterday | CORE-SURVIVES | `ViewModels/DailyReportViewModel.cs:72` — single delegating call, `CanEditSelectedDay = _service.CanEditDay(SelectedDate)` | `TimesheetApp.Core/Services/StandupService.cs:34-38` (`CanEditDay`), enforced again server-side in `AddEntryAsync`/`UpdateEntryAsync`/`DeleteEntryAsync`/`ReorderEntryAsync` (lines 98,159,181,195) | The rule itself is 100% in Core; WPF VM just reads the boolean result. |
| **Owner-gate**: entry writes restricted to the entry's own owner | CORE-SURVIVES | n/a (no owner check found in WPF ViewModels/Dialogs) | `TimesheetApp.Core/Services/StandupService.cs:158` (`UpdateEntryAsync`: `existing.UserId != (_currentUser.Current?.Id ?? 0) → return false`), line 180 (`DeleteEntryAsync`), line 194 (`ReorderEntryAsync`) | Server-enforced ownership check, independent of any UI. |
| Issue edits are **collaborative / not owner-gated** (intentional) | CORE-SURVIVES | n/a | `TimesheetApp.Core/Services/IStandupService.cs:38,43-53` (doc comments: "Issues are collaborative... anyone may edit anyone's issue"), `StandupService.cs:230-248` (`UpdateIssueAsync` / `UpdateIssueCheckedAsync` — no owner check present) | `UpdateIssueCheckedAsync` (M8.3) is explicitly documented as "the web API's write path", i.e. this exact rule already has a web caller. |

## Web-side confirmation (supporting, not required for verdicts above)

Since every rule is CORE-SURVIVES on its own merits, web parity is not load-bearing for the verdict, but it is present and worth recording as corroboration that the intended behavior continues to be exercised end-to-end:
- Angular models: `src/timesheet-web/src/app/api/models/standup-entry-dto.ts`, `standup-issue-dto.ts`, `settings-standup-entry-update-request.ts`, `settings-standup-reorder-request.ts`, `settings-standup-issue-update-request.ts`.
- Angular pages: `src/timesheet-web/src/app/pages/daily-report/daily-report.component.ts`, `standup-entry.ts`, `standup-day.ts`.
- API generated fn wrappers calling into the same `IStandupService`/`StandupRepository` stack: `src/timesheet-web/src/app/api/fn/standup/{standup-entry-create,standup-entry-update,standup-entry-delete,standup-entry-reorder,standup-issue-create,standup-issue-update,standup-issue-delete,standup-board,standup-my-day,standup-quick-import,standup-archive-week}.ts`.

No web-side gap analysis was needed for a COVERED/PARTIAL/MISSING verdict in this section because every rule audited is CORE-SURVIVES — the deciding evidence is that the rule's implementation is physically inside `TimesheetApp.Core`, a directory M10 does not delete.
