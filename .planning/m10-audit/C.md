# Section C — Schema (user_version 9→11, 16+ tables, connection policy)

**Section risk:** None — this entire section is data-layer definition (`DatabaseInitializer.cs`, `SqliteConnectionFactory.cs`, `Models/*.cs`, `Data/Repositories/*.cs`), which lives 100% in `src/TimesheetApp.Core/`. M10 deletes `src/TimesheetApp/` (WPF UI project) only; Core is untouched and is already the dependency both the WPF app and the API/Angular stack build on, so nothing in this section can be lost by the deletion.

## Method note
Every sub-feature below was checked against the two-step test: (a) where does it live, (b) if Core → `CORE-SURVIVES` and stop. All 18 items in this section resolved to Core on the first check — none required hunting for a web equivalent, because nothing here dies. One doc-staleness finding is flagged separately (does not affect the deletion verdict, but is worth a state-file correction).

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| C1a. `PRAGMA user_version` migration mechanism (forward-only, additive, gated) | CORE-SURVIVES | — | `src/TimesheetApp.Core/Data/DatabaseInitializer.cs:225-370` (`RunMigrations`) | [VERIFIED] Pure Core class; WPF only calls `IDatabaseInitializer.InitializeAsync()` via DI (`src/TimesheetApp/App.xaml.cs:51,165`). |
| C1b. v1→v9 migration steps as documented (Requests→Backlogs rename, enum normalization, assignee_user_id, Daily Report bump, Backlog tracking cols, team_id, Task type/assignee) | CORE-SURVIVES | — | `DatabaseInitializer.cs:230-304` (migrations array, steps v1-v9) | [VERIFIED] All steps are `Action<IDbConnection, IDbTransaction>` lambdas inside Core. |
| C1c. Seed: `EnsureDefaultBacklog` (hidden `DEFAULT`/`DEFAULT` record, idempotent) | CORE-SURVIVES | — | `DatabaseInitializer.cs:372-383` | [VERIFIED] |
| C1d. Seed: `SeedDefaultTasksIfEmpty` (`Annual Leave, Meeting, Other`, only when table empty) | CORE-SURVIVES | — | `DatabaseInitializer.cs:385-400` | [VERIFIED] |
| C2a. 18-table schema DDL (Users, Teams, UserTeams, Backlogs, BacklogAudit, Tasks, TaskAudit, TimeLogs, TaskTemplates, DefaultTasks, Settings, StandupEntries, StandupIssues, Tags, BacklogTags, TaskTags, PcaContacts, Holidays) | CORE-SURVIVES | — | `DatabaseInitializer.cs:38-192` (`CreateTables`) | [VERIFIED] Doc says "16" tables but the DDL + migrations produce 18 (doc undercounts; not a coverage risk). |
| C2b. `Backlogs` has NO `is_active` — no soft-delete on backlog rows | CORE-SURVIVES | — | `DatabaseInitializer.cs:38-192` — no `is_active` column ever added to `Backlogs` in DDL or any migration step | [VERIFIED] Schema constraint, not app logic; lives entirely in the Core DDL. |
| C2c. `BacklogAudit` FK `RESTRICT` to `Backlogs`, `note` column added in v9 (deadline-change reason) | CORE-SURVIVES | — | v9 step: `DatabaseInitializer.cs:298-304`; `RequestAudit`/`BacklogAudit` FK defined at `DatabaseInitializer.cs:211-221` | [VERIFIED] |
| C2d. `TimeLogs` `UNIQUE(user_id, task_id, work_date)` — natural key used for upsert | CORE-SURVIVES | — | `DatabaseInitializer.cs:73-83` (DDL), consumed at `Data/Repositories/TimeLogRepository.cs` (upsert queries) | [VERIFIED] |
| C2e. `StandupIssues` FK `ON DELETE CASCADE` to `StandupEntries` | CORE-SURVIVES | — | `DatabaseInitializer.cs:118-127` | [VERIFIED] |
| C2f. `Tags` — hard delete only (no `is_active` column) | CORE-SURVIVES | — | `DatabaseInitializer.cs:133-139` — no `is_active` on `Tags` | [VERIFIED] |
| C2g. `PcaContacts` — soft delete via `is_active` | CORE-SURVIVES | — | `DatabaseInitializer.cs:147-151` | [VERIFIED] |
| C2h. Optimistic concurrency: doc claims "NO version/rowversion/updated_at column on ANY table ⇒ silent lost update" | CORE-SURVIVES (doc is stale) | — | `DatabaseInitializer.cs:305-338` — v10 migration ADDs `row_version INTEGER NOT NULL DEFAULT 1` to `Backlogs, Tasks, TimeLogs, StandupIssues, Users, Teams, Tags, PcaContacts` | [VERIFIED] **Doc discrepancy, not a deletion risk**: the inventory (written against schema v9) is out of date — current `SchemaVersion = 11` (`DatabaseInitializer.cs:15`) already added `row_version` columns plus `Users.password_hash/is_admin/active_team_id` (v10) and a case-insensitive unique username index (v11) as explicit "M8.2 web foundation" work. This is schema evolution already living in Core; flagging for a STATE.md/inventory correction, not a web-coverage gap. |
| C2i. Hardcoded enums: `BacklogProjects`, `BacklogType`, `TaskStatus`/`StandupStatus`, `StandupIssueStatus`, `StandupSection`, `ScheduleState` | CORE-SURVIVES | — | `Models/Entities.cs`, `Models/StandupModels.cs`, `Models/ReadModels.cs:118` (`ScheduleState`) | [VERIFIED] `ScheduleState` additionally confirmed already mirrored into the Angular DTO layer: `src/timesheet-web/src/app/pages/task-list/task-list.model.ts:17` cites `Core/Models/ReadModels.cs:118` by name for its serialization contract — direct evidence Core enums are the single source of truth consumed by the web stack too. |
| C2j. Storage convention: dates as ISO TEXT (`work_date`/`deadline`=`yyyy-MM-dd`; `created_at`/`changed_at`=UTC `yyyy-MM-ddTHH:mm:ssZ`) | CORE-SURVIVES | — | e.g. `DatabaseInitializer.cs:381` (`DateTimeOffset.UtcNow.ToString("yyyy-MM-ddTHH:mm:ssZ")`) | [VERIFIED] Convention enforced at the Core repository layer, not the WPF UI. |
| C2k. Storage convention: `hours` stored `REAL`, bound as `double`, narrowed to `decimal` at the boundary (XC-01) | CORE-SURVIVES | — | `Data/Repositories/TimeLogRepository.cs:158,164,234,243` (`(decimal)r.hours`) | [VERIFIED] Narrowing happens inside the Core repository, before any UI (WPF or Angular) ever sees the value. |
| C2l. `period_month` = `yyyy-MM`, compared lexically; retention logic depends on this | CORE-SURVIVES | — | `Services/StandupArchiveService.cs` (retention), `DatabaseInitializer.cs` migration v2/v7 columns | [VERIFIED] |
| C2m. Settings keys `chua_log_n_days` (3), `retention.pruned_through`, `entry.collapseAll` | CORE-SURVIVES | — | `Settings` KV table defined `DatabaseInitializer.cs:92-95`; consumed by Core settings/retention services | [VERIFIED] Table + convention is Core; the *value* `entry.collapseAll` is UI-preference state but the storage mechanism (KV table, key naming) is Core-owned regardless of which UI writes to it. |
| C3. Connection policy — `Mode=ReadWriteCreate`, `ForeignKeys=true`, `Pooling=false`, `journal_mode=DELETE` (Desktop/OneDrive-safe) | CORE-SURVIVES | — | `Data/SqliteConnectionFactory.cs:39-88` (`SqliteProfile.Desktop` branch, lines 68-73) | [VERIFIED] This is literally the class the WPF app's DI registers (`src/TimesheetApp/App.xaml.cs:137`), but the class itself lives in Core — only the *registration line* dies with the WPF project, not the implementation. |
| C3-directive. Doc's own migration directive: "on server, delete XC-08/09/10/Pooling=false/journal_mode=DELETE; turn on WAL + pooling" | CORE-SURVIVES — **already implemented** | — | `SqliteConnectionFactory.cs:13-26` (`SqliteProfile` enum, `Server` case) + `:58-67` (WAL/pooling/busy_timeout/synchronous=NORMAL for Server profile); wired live at `src/TimesheetApp.Api/Program.cs:76` (`new SqliteConnectionFactory(..., SqliteProfile.Server)`) | [VERIFIED] The directive the inventory doc asks for has already been executed as part of the M8.2 web foundation work — the API is running today with the Server profile (WAL, pooling=true, busy_timeout=1000). Not a gap; confirms the design already anticipated and satisfied this requirement inside Core. |

## Summary
- **18/18 features: CORE-SURVIVES.** Nothing in Section C (schema definition, migrations, seeding, enums, storage conventions, connection policy) is implemented in `src/TimesheetApp/`. It all lives in `src/TimesheetApp.Core/Data/` and `src/TimesheetApp.Core/Models/`, which M10 does not touch.
- **No MISSING/PARTIAL items** — there was nothing to hunt for in the web app for this section, because the WPF project never owned any of this behavior in the first place; it only ever *called into* Core.
- **One doc-staleness flag (C2h):** the inventory's "no optimistic concurrency" claim was true at schema v9 but is now false at the current schema v11 — `row_version` columns and a `Users` auth/team block were added by the v10/v11 migrations already present in Core. Recommend a one-line correction to the inventory doc or a note in STATE.md; this does **not** change the M10 deletion verdict.
- **One directive-already-satisfied flag (C3-directive):** the doc's own "when you go to server, flip to WAL+pooling" instruction has already been executed — `SqliteProfile.Server` exists and is wired into the live API (`Program.cs:76`). Worth surfacing so nobody re-does this work under M10/M11.
