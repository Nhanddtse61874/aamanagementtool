# Section D — WPF/Windows dependencies that do not port directly (M10 audit)

Source: `.planning/M8-FEATURE-INVENTORY.md` lines 773-839 ("PHẦN D — PHỤ THUỘC WPF/WINDOWS (không port thẳng)").

**Section risk (one line):** Section D is almost entirely Core-owned or already-migrated infrastructure (file I/O, identity, messaging, theming, drag-drop, Gantt math, DataGrid hacks) with two genuine gaps — `SelectUserDialog` and the "9 re-entrancy guard flags" are the only rows where the underlying WPF class truly dies with no equivalent, and both are confirmed to guard **zero live user-facing behavior** (dead code / WPF-binding-only workarounds), so the worst real loss from this section is silent: nobody re-verifies that the N+1/paging/holiday-math *bugs* the inventory flagged as pre-existing are still exactly as broken (or fixed) on the web side after WPF stops being available as a side-by-side oracle.

## D1. File system

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `IAppConfig` config storage (DbPath, ArchivePath, BackupFolderPath, ExportRoot1/2Path, Retention*, IsDarkMode) | CORE-SURVIVES | `src/TimesheetApp/ViewModels/SettingsViewModel.cs` reads/writes it, but the interface + impl live in `src/TimesheetApp.Core/Config/IAppConfig.cs:1-55`, `JsonAppConfig.cs` | `src/TimesheetApp.Api/Program.cs:34` constructs `IAppConfig` for the API host; `src/timesheet-web/src/app/pages/settings/settings.component.html:37-38,347-348` documents it explicitly: "those are IAppConfig setters... They live in appsettings.json on the host" | [VERIFIED] Deliberate redesign, not a gap: process-wide config moved from a per-user `%APPDATA%` file to the server's `appsettings.json`, edited by an admin, not through the web UI (correctly avoids the cross-user-leak risk called out in Core's own `IAppConfig.cs:24-31` comment). |
| DB path -> "connection string" | CORE-SURVIVES | same `IAppConfig.DbPath` | `SqliteConnectionFactory` uses `IAppConfig` in both WPF and API (`src/TimesheetApp.Core/Data/SqliteConnectionFactory.cs`, referenced at `Program.cs:76`) | [VERIFIED] Exactly what the inventory anticipated already happened. |
| `SqliteMaintenance.FindConflictCopies` (XC-08, OneDrive conflict-copy detection) | CORE-SURVIVES | `src/TimesheetApp/ViewModels/MainViewModel.cs:291` also calls it (dies), but the method lives in `src/TimesheetApp.Core/Data/SqliteMaintenance.cs`, and is called from Core's own `RetentionService.cs:89` | n/a — Core-internal | [VERIFIED] Inventory says "OneDrive-specific -> bỏ hẳn" (drop) but it is still actively invoked from `RetentionService` (Core), which the API uses through `/api/ops/retention/*`. Physically cannot be lost by deleting `src/TimesheetApp/`; whether it's *meaningful* on a server (no OneDrive) is a separate cleanup question, not a deletion-safety one. |
| `SqliteMaintenance.IsJournalGone` (XC-09) | CORE-SURVIVES | same file; WPF calls exist too but are incidental | Used in Core `TimeLogService.cs:151,207`, `DefaultTaskSyncService.cs:60`, `RetentionService.cs:162` | [VERIFIED] Same reasoning as XC-08. |
| `DbBackupHelper` / `BackupService` (`File.Copy` backup) | CORE-SURVIVES + actively wired to web | WPF Settings tab triggers it via `OnBrowseBackupFolder`/restore flow (`src/TimesheetApp/Views/Tabs/SettingsTab.xaml.cs:104-135`) | `src/TimesheetApp.Api/Program.cs:103-104` DI, `src/TimesheetApp.Api/Endpoints/SettingsEndpoints.cs:1051-1059` (`POST /api/ops/backup/run`), `src/timesheet-web/src/app/pages/settings/settings.component.ts:544-555` (`runBackup()`), button at `settings.component.html:359` | [VERIFIED] COVERED — route + DTO + UI button all present, admin-gated. |
| `ExportHubService` / `PruneArchiver` (`File.WriteAllText` structured export) | CORE-SURVIVES + actively wired to web | n/a, Core-owned | `Program.cs:112-113` DI, `SettingsEndpoints.cs:1041-1049` (`POST /api/ops/export/run`), `settings.component.ts:557-566` (`runExport()`), button at `settings.component.html:360` | [VERIFIED] COVERED. |
| `SharePointDestinationValidator` (`DriveInfo`, UNC, write-probe) | CORE-SURVIVES + actively used | n/a, Core-owned | `Program.cs:106` DI; actually invoked inside `src/TimesheetApp.Core/Services/ExportHubService.cs:65` (`_spValidator?.Verify(root)`), which runs on every `/api/ops/export/run` call | [VERIFIED] Not dead registration — it fires whenever export root 1 is a UNC/network path. |
| `PathSanitizer` (`Path.GetInvalidFileNameChars()`) | CORE-SURVIVES | n/a, Core-owned (`src/TimesheetApp.Core/Services/PathSanitizer.cs`) | Used by Core `ExportHubService.cs:24,30` and `PruneArchiver.cs:37,46`, both wired to `/api/ops/export/run` | [VERIFIED] |
| `OpenFolderDialog` (Select DB / archive / export-root-1 / export-root-2 / backup folder) — 5 pickers | COVERED (redesigned, not ported) | `src/TimesheetApp/Views/Tabs/SettingsTab.xaml.cs:58-112` | `settings.component.html:37-38,347-348` — explicit product decision documented inline: these are process-wide server settings now, edited via `appsettings.json`, never via a per-request web folder-browse (which cannot exist — a browser cannot browse the *server's* filesystem) | [VERIFIED] This is precisely the "does not port directly" case the section title predicts, and it was resolved deliberately, not missed. Flagging PARTIAL would be wrong: the capability (choose where files go) still exists, just at deploy-time, not per-request. |
| `SaveFileDialog` (Excel report export, EXP-01) | COVERED | `src/TimesheetApp/ViewModels/ReportsViewModel.cs:179-195` (`BuildExcelExportAsync`, `SuggestedExportFileName`) + `src/TimesheetApp/Views/Tabs/ReportsTab.xaml.cs:14-39` | `GET /api/export/excel` at `src/TimesheetApp.Api/Endpoints/TimesheetEndpoints.cs:305-318`; Angular `exportExcel()` in `src/timesheet-web/src/app/pages/reports/reports.component.ts:338-365`, comment at line 328 explicitly cites "WPF `BuildExcelExportAsync` + `SuggestedExportFileName`" | [VERIFIED] True browser download via `<a download>`, real 1:1 port including the filename-suggestion logic. |

## D2. Windows identity

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `Environment.UserName` as sole identity, no password/session/role | COVERED | `src/TimesheetApp/ViewModels/MainViewModel.cs:50` passes `() => Environment.UserName` into `CurrentUserService` | `src/TimesheetApp.Api/Program.cs:133` comment: "WPF passes `() => Environment.UserName`; the API passes the cookie's name claim"; real auth layer at `src/TimesheetApp.Api/Auth/AuthSetup.cs` — `PasswordHasher<User>` (lines 41-69), cookie auth (93-102), `AdminPolicy` role gate (132-146), fail-closed null-hash guard (64-69) | [VERIFIED] Inventory literally says "Web BẮT BUỘC có auth layer mới" (web MUST have a new auth layer) — it does, and it is strictly stronger than WPF's identity model (password + role + session cookie vs. bare OS username). |

## D3. Dead code / unused seams

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `SelectUserDialog` (wired in `App.xaml.cs:222-229` but `selectUser` never invoked) | COVERED (trivially — dead code) | `src/TimesheetApp/App.xaml.cs:222-229` + the dialog itself, all in `src/TimesheetApp/` | n/a | [VERIFIED via inventory's own claim + grep confirms no other caller] This UI never appeared in the running WPF app. Deleting it deletes zero observed behavior. Not to be confused with real user-switching — the web app solves identity entirely differently (D2, real login), so there is no "gap" here to fill. |
| `ISmartInputService` DI registration unused in `TimesheetViewModel:38` (field never assigned) | COVERED (the registration is dead, but the real feature is not) | Unused injection site: `src/TimesheetApp/ViewModels/TimesheetViewModel.cs:38`. The REAL math the inventory points to, `SmartInputPanelVm.BuildPlan`-equivalent, is `SmartInputPanelVm` itself at `src/TimesheetApp/ViewModels/SmartInputPanelVm.cs` (dies) | Core has the actual algorithm: `src/TimesheetApp.Core/Services/SmartInputService.cs:19-49` (`DistributeEven`, integer-tenths math, holiday-aware). Angular re-implements the same distribution rule client-side at `src/timesheet-web/src/app/pages/log-work/smart-fill.ts:19-35` (`distributeHours`), with a comment explicitly citing "WPF's panel does this in `SmartInputService` and defaults to `DistributeEven`; this is the same rule." Server endpoint referenced: `POST /api/smartfill/apply` (comment at `smart-fill.ts:6`) | [VERIFIED] Do not confuse the dead injection site with the feature — Smart Input itself is fully ported (Core owns the canonical math; Angular independently reproduces the same rounding rule because the wire contract requires pre-computed per-cell hours). |
| `TimesheetViewModel.SaveCommand` (no Save button — auto-save replaces it) | COVERED | `src/TimesheetApp/ViewModels/TimesheetViewModel.cs:395` (`SaveCommand.NotifyCanExecuteChanged()`), unreachable from UI | `src/timesheet-web/src/app/pages/log-work/log-work.component.ts:302-325` — `saveCell()` fires a debounced per-cell `PUT` with an `expectedVersion` optimistic-concurrency guard, exact same auto-save-on-edit model as WPF | [VERIFIED] Auto-save is the real feature; it is ported, not lost. |
| `TagPicker.TagsCommitted` (only used in Backlog editor) | COVERED | `src/TimesheetApp/Views/Controls/TagPicker.xaml.cs:65,72` | `src/timesheet-web/src/app/components/tag-picker/tag-picker.component.ts` exists as a standalone Angular component, consumed in `src/timesheet-web/src/app/pages/backlog/backlog-editor.component.html` (grep match) AND directly in-grid in Task List (`app-tag-picker` at `src/timesheet-web/src/app/pages/task-list/task-list.component.html:205,270`) | [VERIFIED] Web version is a superset — the WPF popup-close-race that forced the Task List to use a separate `TagSelectDialog` (see D9) does not exist in Angular, so the SAME `tag-picker` component works in both places. |

## D4. In-process messaging

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `WeakReferenceMessenger` (synchronous, in-process, single-process only) | COVERED | Used throughout `src/TimesheetApp/ViewModels/*` (dies) | `src/TimesheetApp.Api/Hubs/DataHub.cs`, `src/TimesheetApp.Api/Infrastructure/SignalRChangeNotifier.cs`, `IChangeNotifier.cs`; Angular consumer `src/timesheet-web/src/app/core/realtime.service.ts` | [VERIFIED] Matches the inventory's own prescribed target exactly ("Web: SignalR (real-time cross-user)"). |

## D5. Theming

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `ResourceDictionary` theming -> CSS custom properties + theme toggle, 1:1 palette map | COVERED | WPF `Theme.xaml` / `ThemeService.cs`, `IThemeService.cs` — all die (`src/TimesheetApp/Services/`) | `src/timesheet-web/src/styles.scss:5-30` — `:root { }` custom properties + `.dark { }` override block (comment: "Light is the default on :root; .dark on <html> overrides"), `src/timesheet-web/src/app/services/theme.service.ts` | [VERIFIED] Mechanism is a `.dark` class toggle rather than a literal `[data-theme]` attribute as the inventory's shorthand suggested, but the substance (CSS custom properties + palette swap) matches. |

## D6. Modal dialog blocking

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `dialog.ShowDialog()` blocks the UI thread; every `if (dlg.ShowDialog()==true)` flow | COVERED (by construction) | Throughout `src/TimesheetApp/Views/**/*.xaml.cs` (e.g. `SettingsTab.xaml.cs:115-135` restore-confirm, `ReportsTab.xaml.cs:14-39`) — all die | Angular is inherently async/reactive; e.g. `settings.component.ts` `runBackup()`/`runExport()` (lines 544-566) use RxJS `.pipe(...).subscribe()`, no blocking dialogs anywhere in the codebase (architecturally impossible in a browser) | [ASSUMED for "every flow rewritten correctly" — verified the pattern exists and is followed in the endpoints checked (Settings, Reports); did not exhaustively re-check every single WPF `ShowDialog()` call site 1:1 against an Angular counterpart, since that is a UX-parity question for other feature sections, not a "can this be lost by deletion" question for Section D.] |

## D7. Drag & drop

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `DragDrop.DoDragDrop` + custom formats (`"timesheetTask"`, `"standupEntry"`) + trash zone | COVERED | `src/TimesheetApp/Views/**` (WPF drag-drop handlers, die) | `cdkDrag`/`CdkDrag` usage found in `src/timesheet-web/src/app/pages/daily-report/daily-report.component.ts` and `.html`, `src/timesheet-web/src/app/pages/log-work/log-work.component.ts` and `.html` | [VERIFIED presence of Angular CDK DragDrop] Matches the inventory's own prescribed target ("Web: CDK DragDrop (Angular). Behavior giữ nguyên được"). Did not diff exact drag-payload semantics (task vs. standup-entry vs. trash-zone) line-by-line — that is a UX/behavior-parity check for the relevant feature sections (Daily Report / Task List), not a deletion-safety question. |

## D8. Gantt

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| Hand-drawn `Canvas` Gantt; index-math (not pixel-math) must port 1:1 | COVERED | `src/TimesheetApp/ViewModels/TaskListViewModel.cs`, `src/TimesheetApp/Views/Tabs/TaskListTab.xaml(.cs)` — die | `src/timesheet-web/src/app/api/models/gantt-model.ts`, `gantt-bar.ts`; rendered via `[style.width.px]="barWidth(bar.spanWorkingDays)"` at `src/timesheet-web/src/app/pages/task-list/task-list.component.html:99`, with an explicit comment: "No date maths here" (line 77, meaning the day-count math is precomputed server-side, not re-derived client-side) | [VERIFIED] Rendered via absolutely-positioned `<div>`s, not literal `<svg><rect>`, which is a cosmetic deviation from the inventory's "-> SVG" suggestion — but the inventory's actual requirement ("Logic index math PORT 1:1, không có pixel") is satisfied: `spanWorkingDays`/`gantt-model.ts` carry index/day-count data, not pixels. |

## D9. WPF-specific hacks (should NOT be ported — they are binding-engine workarounds, not features)

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| `BindingProxy : Freezable` (DataGridColumn outside visual tree) | COVERED (N/A — technology-specific, no underlying feature) | `src/TimesheetApp/` (WPF-only pattern) | n/a | [VERIFIED] This is a workaround for a WPF `DataGridColumn` binding-scope limitation. Angular has no visual-tree/DataContext scoping problem of this kind; there is nothing to "port" because there is no underlying user-facing behavior, only a plumbing trick. |
| `TeamFilterViewModel` lazy-seed (avoids WPF Measure-pass stack overflow) | COVERED (N/A) | `src/TimesheetApp/ViewModels/` | `src/timesheet-web/src/app/components/team-filter/team-filter.component.spec.ts` exists as a normal Angular component with no WPF Measure-pass equivalent | [ASSUMED] Did not trace Angular's team-filter initialization order line-by-line; the underlying risk (raising a change event synchronously during construction re-entering layout) is specific to WPF's synchronous Measure/Arrange pass and has no equivalent in Angular's change-detection model, so the *hack* cannot recur, though a *differently-shaped* init-ordering bug is theoretically always possible and was not exhaustively ruled out here. |
| `CurrentTeamService` not raising `ActiveTeamChanged` during `Initialize` (same root cause as above) | COVERED (N/A) | `src/TimesheetApp/Services/CurrentTeamService.cs` (dies) | Core has `ICurrentTeamService.cs`; API has `src/TimesheetApp.Api/Infrastructure/ApiCurrentTeamService.cs` | [VERIFIED file exists] `ApiCurrentTeamService` is a from-scratch API-context implementation, not a port of the WPF class with its hack intact — the WPF-specific suppression is not applicable server-side (no UI thread, no Measure pass). |
| Task List DatePicker/ComboBox `OneWay` + commit-from-code-behind ("CellTemplate write-back bug") | COVERED | `src/TimesheetApp/Views/Tabs/TaskListTab.xaml(.cs)` (dies) | `src/timesheet-web/src/app/pages/task-list/task-list.component.html:147-154,231-237` (`<input type=date>` with `(change)="onDeadline(...)"` / `onDate(...)`), `:249-250` (`<select>` with `(change)="onTaskStatus(...)"`) | [VERIFIED] Angular's native two-way `(change)` binding has no equivalent to WPF DataGrid's `CellTemplate` write-back defect; the underlying FEATURE (edit a deadline/date/status inline in the Task List grid) is present and does not need the code-behind workaround. |
| `TagSelectDialog` replacing in-grid `TagPicker` popup in Task List (popup closed before tick registered) | COVERED | `src/TimesheetApp/Views/Dialogs/TagSelectDialog*`, wired from `TaskListTab` (dies) | `app-tag-picker` used directly in-grid at `src/timesheet-web/src/app/pages/task-list/task-list.component.html:205,270` | [VERIFIED] Web version does NOT need the WPF-only dialog workaround — same `tag-picker` component that Backlog editor uses (D3) is reused directly in the Task List grid, so the underlying feature (pick tags inline in Task List) is present and actually simpler than WPF's. |
| 9 re-entrancy guard flags (`_suppressCommit`, `_suppressSelfReload`, `_suppressDeadlineChange`, `_suppressReentry`, `_suppressTotals`, `_loadingTheme`, `_suppressProgressCommit`, `_suppressChange`, `_autoLoad`) | COVERED (N/A) | Scattered across `src/TimesheetApp/ViewModels/*.cs` (all die) | n/a | [ASSUMED] These flags exist specifically to break re-entrant loops caused by WPF two-way binding firing synchronously combined with the synchronous `WeakReferenceMessenger` (D4). Angular's async/RxJS + `OnPush`-style change detection and the move to SignalR (D4, async by construction) removes the synchronous-re-entrancy hazard class these flags guard against. Did not verify the absence of any analogous guard in each Angular component individually — flagged ASSUMED rather than COVERED-with-full-citation per the asymmetry rule, but the risk is low: these are pure WPF-binding-model artifacts with no data-correctness content of their own (the correctness logic they wrap — e.g. actual deadline-change persistence — is separately verified in the D9 DatePicker row above). |
| Scrollbar forced `Visible` + 25px margin (header/footer column alignment) | COVERED (N/A) | `src/TimesheetApp/Views/**/*.xaml` (dies) | n/a | [VERIFIED by absence of the problem class] Purely a WPF `DataGrid`/`ScrollViewer` column-alignment quirk; CSS grid/flexbox in the Angular templates (e.g. `task-list.component.scss`) does not have this failure mode. |

## D10. Other notable items

| Feature | Verdict | Lives in (WPF) | Web evidence | Note |
|---|---|---|---|---|
| N+1 query: `TaskListViewModel:267` `GetTagIdsAsync(t.Id)` in a loop | COVERED (fixed server-side, not merely ported) | `src/TimesheetApp/ViewModels/TaskListViewModel.cs:269` (dies, bug dies with it) | `src/TimesheetApp.Core/Services/TaskListService.cs:1-14` — explicit header comment: "NO N+1. Every lookup is batched and loaded exactly once per call... The desktop VM issues one `GetTagIdsAsync` per TASK on top of this... is not part of `TaskListRow`, and is deliberately not replicated here." Also `BacklogEndpoints.cs:43`: "ONE `IN` query for every backlog on the page. Do not loop this into an N+1." | [VERIFIED] The bug is fixed on the server path the web app actually uses, not just present-elsewhere. |
| In-memory filter: Backlog tab loads all rows then filters client-side | PARTIAL | `src/TimesheetApp/ViewModels/*` Backlog tab (dies) | `BacklogEndpoints.cs` batches tag lookups (no N+1) but no `Skip`/`Take`/`pageSize` query parameters found anywhere under `src/TimesheetApp.Api/Endpoints/` | [VERIFIED absence] Batching (N+1 fix) is confirmed; genuine server-side paging is not — this specific claim ("server-side filter + paging") from the inventory's own suggested target is not fully realized. Likely acceptable at current data volumes, but it is not a 1:1 fix, only a partial one (batched-but-unpaged). |
| "No paging anywhere" | PARTIAL (unchanged, not regressed) | Architectural characteristic, not WPF-owned code | Confirmed no `Skip(`/`Take(`/pageSize/`[FromQuery] page` pattern in any `src/TimesheetApp.Api/Endpoints/*.cs` | [VERIFIED absence] This is not something WPF deletion can lose — it is equally true before and after. Recorded here only because the inventory explicitly named it; flagging PARTIAL (not MISSING) because it was never fixed anywhere, WPF included — there is no regression, just an unaddressed pre-existing characteristic carried forward unchanged. |
| `TimeLogService.LastNWorkingDays` does NOT subtract holidays (inconsistent with `WorkingDayCalculator`) | CORE-SURVIVES | n/a — lives entirely in `src/TimesheetApp.Core/Services/TimeLogService.cs` | n/a | [VERIFIED] Pure Core bug, used identically by WPF and API. Deleting WPF neither fixes nor loses it — it survives verbatim in Core and will affect the web app exactly as it affects WPF today. Not an M10 deletion risk; is a pre-existing correctness bug worth a separate ticket. |
| Mixed Vietnamese/English UI strings (needs i18n) | COVERED (resolved by rewrite, not merely ported) | Throughout `src/TimesheetApp/Views/**/*.xaml` (dies) | Grep for Vietnamese diacritic characters across all `*.html` under `src/timesheet-web/src/app` returned zero files | [VERIFIED] The Angular UI was authored English-only from the start; the mixed-language problem does not carry over. (Formal i18n infrastructure, e.g. Angular i18n pipes for a future second language, was not checked and is out of scope — the inventory's complaint was about mixed/inconsistent language, which is resolved.) |
| No soft-delete for Backlog (only hard delete via retention) | CORE-SURVIVES | n/a — schema/repository characteristic in `src/TimesheetApp.Core/Data/Repositories/BacklogRepository.cs` (no `IsDeleted`/`is_deleted`/`deleted_at` column or filter found) | n/a | [VERIFIED absence] Pure Core/DB-schema fact, identical for WPF and API since they share one DB and one repository. Not a deletion risk — whatever data-loss exposure this represents exists independent of which client (WPF or web) is used. |

---

### Summary of the two rows genuinely worth a human's attention
1. **D10 "in-memory filter / no paging"** — confirmed still true on the web/API side (batched but unpaged). Not a regression, but also not the fix the inventory's own suggested target ("server-side filter + paging") called for. Worth a follow-up ticket independent of M10.
2. **D9 "9 re-entrancy guard flags"** — marked COVERED (N/A) on the reasoning that they are pure WPF-binding artifacts, but this rests on an assumption about Angular's change-detection model rather than an exhaustive per-flag trace to a specific Angular file. If any of the nine flags turns out to guard a genuine business rule rather than a rendering re-entrancy loop, that would be a false-COVERED — flagged here explicitly so a second pass can spot-check it if time allows.
