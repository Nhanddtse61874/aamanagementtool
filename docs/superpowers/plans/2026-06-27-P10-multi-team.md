---
phase: P10
title: Multi-Team (team scoping, membership, active team, multi-team view)
mode: B
schema_target: 8
pause_before_execute: true   # user must approve this plan before W1 (schema-wide / migration)
must_haves:
  observable_truths:
    - Opening a v7 DB upgrades to v8 (Teams + UserTeams + team_id on Backlogs/StandupEntries) and a post-init bootstrap assigns ALL existing backlogs/standup/users to team "Architect Improvement" (idempotent, backup-first), no data loss.
    - A fresh DB auto-creates an active team "My Team", seeds its per-team DEFAULT + default tasks, and is immediately usable.
    - Team = new entity; Project enum unchanged; a user belongs to ≥1 teams (50-50 = two memberships).
    - One active team (sidebar switcher) scopes editing/creation; Log Work shows only the active team's tasks incl. its DEFAULT.
    - Backlog list, Task List, Reports, Daily Board each have a multi-team checkbox (default = active team) that aggregates checked teams; switching active team resets each filter.
    - Each team has its own DEFAULT backlog; Annual Leave/Meeting attribute to the active team in reports; DefaultTaskSync materializes the global DefaultTasks under every team.
    - Settings can create/rename/deactivate teams and assign users (many-to-many).
    - Reports/exports are team-aware (team grouping/column; RPT-04 banner scoped to active-team members).
    - No exports or grids leak another team's data; all prior ~328 tests stay green.
  required_artifacts:
    - DatabaseInitializer v8 migration + Teams/UserTeams DDL; Team entity + TeamId on Backlog/StandupEntry; team name on TimeLogReportRow (trailing)
    - ITeamRepository (CRUD + UserTeams membership); team params on Backlog/TimeLog/Task/Standup repos; GetDefaultForTeamAsync
    - ICurrentTeamService + JsonAppConfig.ActiveTeamId; ITeamBootstrapService (migration + first-run)
    - DefaultTaskSyncService per-team loop; TimeLogService active-team working scope
    - StandupService team scope; SettingsViewModel Teams section + membership overlay + DataKind.Teams
    - Sidebar switcher (MainWindow/MainViewModel); TeamFilterViewModel + TeamFilter control on 4 screens
    - ExportService/TaskListArchive/StandupArchive/Reports team-aware
    - App.xaml.cs DI + startup order + first-run user→team join
  key_links:
    - team_id NULLABLE; bootstrap runs after InitializeAsync, BEFORE DefaultTaskSync; XC-10 backup before backfill; idempotent "Teams empty?" guard (R3)
    - GetExportRowsAsync/GetReportRowsAsync gain team filter — no cross-team leak (R1)
    - DEFAULT resolved per team via GetDefaultForTeamAsync, never global GetByCodeAsync (R2)
    - Inject ICurrentTeamService directly (NO second Func<int> — would clobber user-id provider)
    - active team validated against user's active memberships on resolve; stale → first available (R5)
    - teamId 0 = empty (not match-all); read APIs optional teamIds=null preserves existing tests (R6)
---

# P10 — Multi-Team · Plan (9 waves). **DO NOT START W1 until the user approves this plan.**

**Refs every task reads:** spec `docs/superpowers/specs/2026-06-27-multi-team-design.md`; architecture map `.planning/research/P10-ARCHITECTURE-RESEARCH.md` (exhaustive per-method list — authoritative for signatures); synthesis `.planning/research/P10-RESEARCH-SYNTHESIS.md` (resolved decisions + risks); `.planning/REQUIREMENTS.md` (TM-01..10); CLAUDE.md (dotnet, surgical, simplicity).
**Config:** Mode B, `parallelization:false` ⇒ sequential dispatch; `commit_atomic:true` ⇒ commit per wave; quality profile. Baseline = 328 tests green.
**Build/test gate each wave:** `dotnet build src/TimesheetApp.sln` (0 err) + `dotnet test src/TimesheetApp.sln` (all green).

> Convention: read APIs gain optional `IReadOnlyList<int>? teamIds = null` (null = all teams = existing behavior); create APIs take required `int teamId`; `teamId 0` treated as empty.
>
> **FIX-3 — test-contract upkeep (every wave that widens a signature):** widening interface methods breaks existing **Moq `.Setup`/`.Verify`** call sites pinned to the old arity, and adding `TeamIds` to `ExportFilter` breaks its `new ExportFilter(...)` sites. Each such wave MUST update those tests as part of its `<verify>` (not treat them as regressions): `ReportsViewModelTests`, `DailyReportViewModelTests`, `RequestsViewModelTests`, `SmartInputPanelVmTests`, `CrossTabSyncTests` (widened repo methods), and `ExportServiceTests` (8 `ExportFilter` ctor sites — add `TeamIds` as a **trailing optional** on the record struct). `ICurrentTeamService` is a **superset** of `ICurrentUserService` (adds `ActiveTeamChanged`/`AvailableTeams`/`InitializeAsync`) — don't copy the user service verbatim. W1's `DatabaseInitializerTests` update includes retargeting the `COUNT(DEFAULT)==1` assertion (DEFAULT creation moves out of the initializer to bootstrap+per-team sync).

## W1 — Data layer + schema (model opus)
Owns: DatabaseInitializer.cs, Entities.cs, ReadModels.cs, TeamRepository.cs+I, BacklogRepository.cs+I, TimeLogRepository.cs+I, TaskRepository.cs+I, StandupRepository.cs+I.
- v8 migration: const 7→8; append step (`ALTER Backlogs/StandupEntries ADD COLUMN team_id INTEGER`); Teams + UserTeams DDL in CreateTables (idempotent). Do NOT touch the v7 step.
- Entities: `Team` record; `TeamId` (nullable) appended to `Backlog` + `StandupEntry`. ReadModels: team id+name **trailing** on `TimeLogReportRow` (no positional reorder — R6).
- `ITeamRepository` + impl (CRUD + soft-delete like Users; UserTeams: GetTeamIdsForUserAsync/GetUserIdsForTeamAsync/SetMembersAsync replace-all/AddMemberAsync; GetByNameAsync for bootstrap).
- Team params per architecture §2: BacklogRepository.SearchAsync(term, teamIds) + team_id in Cols/Insert/Update/Map/Raw + GetDefaultForTeamAsync; TimeLogRepository.GetReportRowsAsync/GetExportRowsAsync(...,teamIds) projecting team id+name (R1); TaskRepository.GetActiveForTimesheetAsync(teamId) (R-critical); StandupRepository team_id on entry + GetEntriesAsync(teamId?)/GetEntriesForDayAsync(teamIds?)/GetEntriesForRangeAsync(teamIds?).
- Tests: v7→v8 upgrade + table/column existence + version pin; ITeamRepository CRUD + membership replace-all; team-filtered SearchAsync/GetExportRowsAsync (leak guard); GetActiveForTimesheetAsync(teamId) scoping; update TestDb.SeedRequestAsync + DatabaseInitializerTests to v8. Keep TimeLogReportRow positional ctor stable.
<verify>build + test green (328 prior with seed updates + new).</verify>

## W2 — Current-team service + bootstrap migration + config (opus)
Owns: CurrentTeamService.cs+I, TeamBootstrapService.cs+I, IAppConfig.cs, JsonAppConfig.cs. Dep: W1.
- `IAppConfig.ActiveTeamId` + Set (mirror BackupKeepCount in JsonAppConfig.Model, backward-compatible default 0).
- `ICurrentTeamService` (architecture §3): AvailableTeams = user's active memberships; resolve persisted id if still available else first; validate stale (R5); SetActiveTeamAsync persists + event + DataKind.Teams; InitializeAsync(userId).
- `ITeamBootstrapService.EnsureBootstrappedAsync` (architecture §1d, spec §3): idempotent guard (Teams empty?); existing-DB path → create "Architect Improvement", backfill backlogs/standup/users, repoint legacy DEFAULT, set active; fresh-DB path → "My Team" active. XC-10 backup before bulk; own transactions; treat teamId 0 as empty.
- Tests: bootstrap migration (seeded v7-state → all rows get team, idempotent re-run, backup taken); fresh-DB first-run creates one active team; CurrentTeamService resolution incl. stale-id fallback + zero-team. (No DI/startup yet — W9.)
<verify>build + test green.</verify>

## W3 — DEFAULT-per-team + working scope (opus)
Owns: DefaultTaskSyncService.cs+I, TimeLogService.cs. Dep: W1, W2.
- DefaultTaskSync per-team loop (architecture §4b): EnsureDefaultBacklogIdAsync(teamId) via GetDefaultForTeamAsync; SyncAsync loops active teams; global DefaultTasks materialized per team (R2 — no global GetByCodeAsync).
- TimeLogService working scope: GetWeekAsync/BuildGroupsAsync/GetWeekGrouped*/GetUsersMissingLogsAsync pass active team (inject ICurrentTeamService); GetWeekGroupedAllUsersAsync passes team to GetExportRowsAsync; RPT-04 banner = active-team members.
- Tests: per-team DEFAULT isolation + no double-count; sync creates DEFAULT for a new team; Log Work grid scoped to active team; RPT-04 team scope.
<verify>build + test green.</verify>

## W4 — Standup team scope (opus)
Owns: StandupService.cs+I. Dep: W1, W2.
- AddEntryAsync stamps active team (inject ICurrentTeamService); GetMyStandupAsync active team; GetTeamStandupAsync(teamIds) + members of checked teams; SearchBacklogsAsync active-team filter.
- Tests: new entry carries active team; board feed filtered by teamIds + team membership.
<verify>build + test green.</verify>

## W5 — Settings Teams UI (opus)
Owns: SettingsViewModel.cs, SettingsTab.xaml(+.cs), TeamMembershipEditorViewModel.cs, DataChangedMessage.cs (add Teams). Dep: W1, W2.
- Team CRUD (rename/deactivate soft-delete, mirror Users/PCA) + membership overlay (user checklist, replace-all SetMembersAsync); broadcast DataKind.Teams; team-create → EnsureDefaultBacklogIdAsync + sync.
- Tests: VM team create/rename/deactivate persists; membership replace-all; create triggers DEFAULT. (UI visuals = UAT.)
<verify>build + test green.</verify>

## W6 — Active-team switcher (opus)
Owns: MainWindow.xaml, MainViewModel.cs. Dep: W2, W5.
- Inject ICurrentTeamService; AvailableTeams + ActiveTeam prop (setter → SetActiveTeamAsync); sidebar ComboBox above the user chip; hide for single-team users; reload working views on ActiveTeamChanged/DataKind.Teams.
- Tests: VM active-team set persists + reloads; available teams = user memberships. (Visual = UAT.)
<verify>build + test green.</verify>

## W7 — Multi-team checkbox filter on 4 screens + backlog-create scope (opus)
Owns: TeamFilterViewModel.cs, Views/Controls/TeamFilter.xaml(+.cs), **RequestsViewModel.cs** (class `BacklogsViewModel`), **RequestEditorViewModel.cs** (class `BacklogEditorViewModel`), TaskListViewModel.cs, ReportsViewModel.cs, DailyReportViewModel.cs, the 4 tab XAMLs. Dep: W1, W2, W6.
- Shared TeamFilterViewModel (TeamCheckVm list, default = active team, session-persist, SelectionChanged); reset to active team on ActiveTeamChanged. Wire each VM's load to pass CheckedTeamIds (`RequestsViewModel.cs:76` SearchAsync, `TaskListViewModel.cs:98`, `ReportsViewModel.cs:154-155`, `DailyReportViewModel.cs:61,66`); show team chip/column when >1 checked.
- **FIX-2 — backlog create with active team (TM-06):** wire `BacklogEditorViewModel`/`BacklogsViewModel` create path to stamp the new `Backlog.TeamId = ICurrentTeamService.ActiveTeamId` (inject the service); editing a backlog keeps its team. Without this, new backlogs persist team_id=NULL and vanish from team grids.
- Tests: each VM loads aggregated rows for checked teams; default = active team; reset on switch; **new backlog persists active team_id**. (Visual = UAT.)
<verify>build + test green.</verify>

## W8 — Reports / export team-aware (opus)
Owns: ExportService.cs, TaskListArchiveService.cs, StandupArchiveService.cs, ReportsViewModel.cs (drill-down Team node — coordinate with W7's ReportsViewModel edits: this wave runs after W7). Dep: W1, W3.
- ExportFilter gains TeamIds; markdown `## {team}` grouping; Excel + tasklist-archive Team column; standup archive groups by team; report tree adds Team top node (collapses when one team).
- Tests: export filtered by team (no leak); markdown team grouping; tasklist archive team column.
<verify>build + test green.</verify>

## W9 — DI + startup wiring + first-run join (sonnet)
Owns: App.xaml.cs, MainViewModel.cs (first-run user→team join — note W6 also edits MainViewModel; W9 runs after). Dep: all.
- Register ITeamRepository, ICurrentTeamService, ITeamBootstrapService (singletons). **Do NOT add a Func<int> for team** (clobbers user-id provider) — services inject ICurrentTeamService.
- Startup order (architecture §6b): InitializeAsync → EnsureBootstrappedAsync → archive backfills → auto-backup → DefaultTaskSync(per-team) → MainViewModel.InitializeAsync → (inside) ResolveCurrentUserAsync then ICurrentTeamService.InitializeAsync; fresh-DB auto-created user → AddMemberAsync(active team).
- Tests: DI resolves MainViewModel/SettingsViewModel/all new services; startup order reasoned; first-run join. Full suite green.
<verify>build + test green; whole suite.</verify>

## Cross-wave overlap notes
`MainViewModel.cs` (W6 switcher, W9 first-run join) and `ReportsViewModel.cs` (W7 filter, W8 tree) are each touched by two waves — sequential (parallelization:false), no same-wave overlap. `DatabaseInitializer.cs` (W1), `App.xaml.cs` (W9) single-owner. After every wave: commit per `commit_atomic`.
