---
phase: P8
title: Task List (tracking, tags, holidays, Gantt)
mode: B
schema_target: 7
must_haves:
  observable_truths:
    - Opening a v6 DB upgrades to v7 (7 new Backlog columns + Tags/BacklogTags/PcaContacts/Holidays) without data loss; re-run is idempotent.
    - Sidebar shows Log Work · Backlog · Task List · Daily Report · Reports (WORKSPACE) + Users · Settings (ADMIN); every item loads its view; Timesheet/Standup/Reports do not regress.
    - A backlog can store internal+external deadline, rough+official estimate, manual progress %, note, PCT assignee (User), PCA contact (from list), and N custom tags.
    - Task List shows, per month, every non-DEFAULT backlog with status/assignees/deadlines/progress/logged-hours/estimate/tag-chips; rows expand to tasks.
    - A backlog within ≤2 working days of its internal deadline AND behind (logged/estimate < workingDaysElapsed/Total) shows an amber warning chip; past the internal deadline (not Done) shows a red late chip; Done shows neither.
    - Settings can create/edit/delete custom tags (icon+color+text), manage PCA contacts (soft-delete), and mark/unmark holidays on a month calendar.
    - Holidays + weekends are excluded from every working-day computation (smart-input, schedule math, ≤2-day window, Gantt axis) via one shared calculator.
    - Task List toggles Grid↔Gantt (start→internal-deadline bars over working days, colored by schedule state, external-deadline marker) with collapsible chart area.
    - Completed months auto-export a markdown overview on startup (incl. a "Moved to next month" section); a manual "Export this month" button exists.
  required_artifacts:
    - DatabaseInitializer v7 migration + CreateTables (Tags/BacklogTags/PcaContacts/Holidays)
    - Entities Tag/PcaContact/Holiday + Backlog v7 fields; ReadModels ScheduleState/TaskListRow/GanttBar/GanttModel
    - ITagRepository, IPcaContactRepository, IHolidayRepository; IBacklogRepository tag-link + v7 cols; ITimeLogRepository.GetLoggedHoursByBacklogAsync
    - IWorkingDayCalculator, IScheduleStateService, ITaskListArchiveService; SmartInputService holiday overloads
    - SettingsViewModel/SettingsTab additions (Tags, PCA, Holiday calendar) + HolidayCalendarViewModel + HexToBrushConverter
    - BacklogEditorViewModel/BacklogsTab tracking fields + tag pick
    - TaskListViewModel + TaskListTab (grid + Gantt canvas)
    - MainWindow/MainViewModel sidebar restructure
    - App.xaml.cs DI + MainViewModel injection + TL-09 startup backfill
  key_links:
    - Schema v7 const+array move together (R1) → upgrade test green
    - Logged-hours join has NO is_active filter (XC-06, R3)
    - ScheduleStateService never throws (R4); shares IWorkingDayCalculator with Gantt axis (R5)
    - New repos use IConnectionFactory per-method (XC-01); pca_contact_id no inline FK
    - Sidebar string-keyed; top-level views routed via OnActiveViewChanged (R2)
    - New DataKind values Tags/PcaContacts/Holidays broadcast on CRUD
---

# P8 — Task List · Implementation Plan (goal-backward, wave-grouped)

**Refs every task assumes are read:** `docs/superpowers/specs/2026-06-27-task-list-design.md` (spec), `.planning/research/P8-RESEARCH-SYNTHESIS.md` (resolved decisions), `.planning/REQUIREMENTS.md` (TL/TAG/HOL REQs), `CLAUDE.md` (code rules: surgical, simplicity-first, stack skill = `dotnet`).
**Config:** `parallelization:false` ⇒ sequential dispatch; `commit_atomic:true` ⇒ commit per wave; profile `quality`.
**Build/test:** `dotnet build src/TimesheetApp.sln` · `dotnet test src/TimesheetApp.sln`. Baseline before W1 = **228 tests green**.

Waves run in order W1→W8 (each depends on prior). Same-wave file-disjoint; the listed file-overlap hotspots are each owned by exactly one wave.

---

## WAVE 1 — Data layer

<task id="W1" model="opus">
<read_first>
src/TimesheetApp/Data/DatabaseInitializer.cs (SchemaVersion const + migrations[] + CreateTables);
src/TimesheetApp/Models/Entities.cs; src/TimesheetApp/Models/ReadModels.cs;
src/TimesheetApp/Data/Repositories/BacklogRepository.cs + IBacklogRepository.cs (Raw/Cols/Insert/Update/audit LogAsync);
src/TimesheetApp/Data/Repositories/TimeLogRepository.cs + ITimeLogRepository.cs (GetReportRowsAsync join);
src/TimesheetApp/Data/Repositories/UserRepository.cs + IUserRepository.cs (soft-delete pattern to mirror);
spec §2,§3; synthesis §A,§D.
</read_first>
<action>
1. DatabaseInitializer: bump SchemaVersion 6→7; append the v7 step as a NEW array element (the migrations[] array already holds indices 0–6 where index 6 is the v6 rename — the v7 step is the 8th element at index 7, runs when user_version<7) with 7 `ALTER TABLE Backlogs ADD COLUMN` (deadline_internal, deadline_external TEXT; rough_estimate_hours, official_estimate_hours REAL; progress_percent INTEGER; note TEXT; pca_contact_id INTEGER). Add the 4 `CREATE TABLE IF NOT EXISTS` (Tags, BacklogTags, PcaContacts, Holidays) into CreateTables. Keep all in the single init transaction.
2. Entities.cs: append Tag, PcaContact, Holiday records; extend Backlog with the 7 nullable-defaulted v7 fields (after AssigneeUserId). Do NOT reorder existing params.
3. ReadModels.cs: append ScheduleState enum, TaskListRow, GanttBar, GanttModel (per spec §2.4).
4. New repos (sealed, IConnectionFactory per-method seam; REAL↔decimal via (double)/(decimal); pca_contact_id no inline FK):
   - TagRepository/ITagRepository: GetAllAsync, InsertAsync→id, UpdateAsync, DeleteAsync (delete BacklogTags links then tag, one tx).
   - PcaContactRepository/IPcaContactRepository: GetAllAsync, GetActiveAsync, GetByIdAsync, InsertAsync→id, UpdateNameAsync, SetActiveAsync (mirror IUserRepository).
   - HolidayRepository/IHolidayRepository: GetAllAsync, GetForMonthAsync(y,m), UpsertAsync(date,desc?), DeleteAsync(date).
5. Extend BacklogRepository: thread the 7 v7 columns through Raw/Cols/Insert/Update; audit each new column via existing LogAsync diff. Add GetTagIdsAsync(id), SetTagsAsync(id, ids) (replace-all one tx), GetTagIdsForAllAsync().
6. Extend TimeLogRepository: GetLoggedHoursByBacklogAsync() → Dictionary<int,decimal>, SUM(hours) GROUP BY t.backlog_id, JOIN Tasks, **NO is_active filter** (XC-06).
7. Tests (src/TimesheetApp.Tests): v6→v7 upgrade test (open a seeded v6 db, run init, assert columns/tables exist + user_version=7 + existing rows intact + idempotent re-run); CRUD round-trip for Tag/Pca/Holiday + BacklogTags set/get + Backlog v7 columns round-trip + audit rows; GetLoggedHoursByBacklogAsync returns soft-deleted-task hours too.
</action>
<verify>dotnet build src/TimesheetApp.sln (0 errors); dotnet test src/TimesheetApp.sln (all prior 228 + new tests green). Automated, <60s.</verify>
<done>v7 migration applies idempotently; all new repos + extensions covered by green tests; no existing test regressed.</done>
</task>

## WAVE 2 — Services (depends W1)

<task id="W2" model="opus">
<read_first>
src/TimesheetApp/Services/SmartInputService.cs (private WorkingDays, DistributeEven, FillFull8h + its tests);
src/TimesheetApp/Services/StandupArchiveService.cs (ExportWeekAsync/BackfillMissingWeeks/FileNameFor/Esc/dir);
src/TimesheetApp/Services/ExportService.cs (markdown shape); src/TimesheetApp/Config/IAppConfig (ArchivePath);
W1 repos; spec §4,§6; synthesis A2/A3, R4/R5.
</read_first>
<action>
1. IWorkingDayCalculator + WorkingDayCalculator (pure): IsWorkingDay(d,holidays), WorkingDaysBetween(from,to,holidays), CountWorkingDays(from,to,holidays) inclusive. No DB/clock.
2. SmartInputService: keep existing DistributeEven(from,to,total)/FillFull8h(from,to) delegating to empty-holiday-set; add holiday-aware overloads taking IReadOnlySet<DateOnly> holidays routed through IWorkingDayCalculator. **Preserve the parameterless ctor** — add an OPTIONAL ctor param `IWorkingDayCalculator? calc = null` defaulting to `new WorkingDayCalculator()`, so `new SmartInputService()` (SmartInputServiceTests.cs:9) and the parameterless DI registration (App.xaml.cs:70) both keep compiling. Existing SI tests MUST stay green (verify SmartInputServiceTests still passes unchanged).
3. IScheduleStateService + ScheduleStateService (pure, never throws): Evaluate(...) per spec §4.3/§6.4 decision table. Guard all divides (total<=0, null/≤0 estimate → not behind/hide). estimate = official ?? rough. ≤2-day window = CountWorkingDays((today,deadline]) ≤ 2.
4. ITaskListArchiveService + TaskListArchiveService (mirror StandupArchiveService): ExportMonthAsync(y,m)→string? (null+no file if no data), BackfillMissingMonthsAsync() startup, FileNameFor(y,m)="{yyyyMM}_tasklist.md", dir = TaskListArchives. Month M = current members (period_month==M, DEFAULT excluded) + moved-out (BacklogAudit field='period_month' old_value=M, not currently M; dedup current-wins) under "Moved to next month". Escape '|'. Pull logged hours (W1), schedule state, tags, assignees, progress.
5. Tests: WorkingDayCalculator (weekend+holiday exclusion, inclusive counts, single-day, holiday inside range, empty range); ScheduleStateService full decision table incl div-by-zero/null estimate/reversed deadline/today-before-start/Done; SmartInput holiday overload (holiday inside range removed from distribution); TaskListArchive (no data→null/no file, moved-out section present, idempotent overwrite, '|' escaped).
</action>
<verify>dotnet build + dotnet test (all green incl new). Automated, <60s.</verify>
<done>Pure calculator + schedule service + archive service implemented and unit-tested; SmartInput holiday path tested; SI-01/02 tests unchanged-green.</done>
</task>

## WAVE 3 — Settings UI: Tags / PCA / Holiday calendar (depends W1, W2)

<task id="W3" model="opus">
<read_first>
src/TimesheetApp/ViewModels/SettingsViewModel.cs; src/TimesheetApp/Views/Tabs/SettingsTab.xaml(+.cs);
src/TimesheetApp/ViewModels/UsersViewModel.cs (CRUD + soft-delete + DataChangedMessage);
src/TimesheetApp/ViewModels/TemplateEditorViewModel.cs + the template overlay XAML (overlay pattern);
src/TimesheetApp/Services/DataChangedMessage.cs (add DataKind values); src/TimesheetApp/Views/Converters/ (8 existing converters; register new ones in App.xaml resources);
W1 repos (Tag/Pca/Holiday); W2 IWorkingDayCalculator; spec §5.5.
</read_first>
<action>
1. DataChangedMessage: add DataKind.Tags, DataKind.PcaContacts, DataKind.Holidays.
2. Converters: add src/TimesheetApp/Views/Converters/HexToBrushConverter.cs (try ColorConverter.ConvertFromString, fallback to a neutral brush on parse failure — never throw). Register in App.xaml resources alongside NullToCollapsedConverter/StringMatch converters.
3. SettingsViewModel: inject ITagRepository, IPcaContactRepository, IHolidayRepository (+IWorkingDayCalculator for calendar weekend marking). Add:
   - Tags: ObservableCollection<Tag>; create/edit (overlay with text + emoji/glyph free-text + quick-pick + color hex + swatches) / delete commands; broadcast DataKind.Tags.
   - PCA: ObservableCollection<PcaContact> (incl inactive); add/rename/deactivate (SetActiveAsync) commands; broadcast DataKind.PcaContacts.
4. HolidayCalendarViewModel (owned by SettingsViewModel): current year/month, prev/next, day cells (working/weekend/holiday), ToggleHolidayCommand → Upsert/Delete; broadcast DataKind.Holidays.
5. SettingsTab.xaml: append three SectionTitle blocks (Tags, PCA contacts, Holiday calendar) + a Tag editor overlay; calendar = UniformGrid of day cells with distinct weekend/holiday styling.
6. Tests: VM-level where feasible (tag/pca command persists via repo; holiday toggle upsert/delete). UI visuals are UAT-only (flag).
</action>
<verify>dotnet build + dotnet test green. Manual visual is UAT (noted). Automated portion <60s.</verify>
<done>Settings can CRUD tags, manage PCA contacts (soft-delete), and toggle holidays on a calendar; changes broadcast; HexToBrushConverter safe on bad hex.</done>
</task>

## WAVE 4 — Backlog editor tracking fields (depends W1)

<task id="W4" model="opus">
<read_first>
src/TimesheetApp/ViewModels/RequestEditorViewModel.cs (BacklogEditorViewModel — assignee combo, month combos, Unassigned sentinel);
src/TimesheetApp/ViewModels/RequestsViewModel.cs (BacklogsViewModel SaveNew/SaveEditAsync);
src/TimesheetApp/Views/Tabs/RequestsTab.xaml (BacklogsTab editor form);
W1 (Backlog v7 fields, SetTagsAsync, IPcaContactRepository, ITagRepository); spec §5.2.
</read_first>
<action>
1. BacklogEditorViewModel: add observable props DeadlineInternal, DeadlineExternal (DateOnly?), RoughEstimateHours, OfficialEstimateHours (decimal?), Note, ProgressPercent (int?, validate 0–100), SelectedPcaContact (+ PcaContacts list incl an Unassigned sentinel mirroring assignee), and a tag multi-select (ObservableCollection<TagPick{Tag,IsChecked}>). Load existing values on edit (incl. GetTagIdsAsync).
2. BacklogsViewModel SaveNew/SaveEditAsync: pass the 7 v7 fields into the Backlog ctor; after insert/update call SetTagsAsync(backlogId, checkedTagIds). DEFAULT backlog: hide/disable tracking UI.
3. RequestsTab.xaml editor: add date pickers, estimate textboxes (numeric), note textbox, progress % input, PCA combo, tag checkbox list. Match existing editor styling/Theme.
4. Tests: SaveNew/SaveEdit round-trips the v7 fields + tags (VM→repo); progress out-of-range rejected.
</action>
<verify>dotnet build + dotnet test green. Editor visual = UAT. Automated <60s.</verify>
<done>Backlog editor persists all tracking fields + tags and reloads them on edit; DEFAULT exempt; audited.</done>
</task>

## WAVE 5 — Task List grid (depends W1, W2)

<task id="W5" model="opus">
<read_first>
src/TimesheetApp/ViewModels/TimesheetViewModel.cs (messenger register static lambda, load pattern);
src/TimesheetApp/ViewModels/RequestsViewModel.cs (month combos, backlog load);
src/TimesheetApp/Views/Tabs/RequestsTab.xaml + ReportsTab.xaml (DataGrid/list styling, chips/pills);
W1 repos + read models; W2 IScheduleStateService/IWorkingDayCalculator; W3 HexToBrushConverter; spec §5.3,§6,§7.
</read_first>
<action>
1. TaskListViewModel (transient): deps IBacklogRepository, ITaskRepository, ITimeLogRepository, ITagRepository, IPcaContactRepository, IUserRepository, IHolidayRepository, IWorkingDayCalculator, IScheduleStateService, IClock, IMessenger. State: Rows (ObservableCollection<TaskListRow>), SelectedYear/Month, IsGantt, IsChartCollapsed, ExportThisMonthCommand. LoadAsync: load month backlogs (period_month==selected, DEFAULT excluded), tasks per backlog, logged-hours dict (all-time), tags (GetTagIdsForAllAsync + tag lookup), assignee/pca names, holidays set; compute ScheduleState (Done predicate per §6.2) per row; build TaskListRow list. Register DataChangedMessage reload on Backlogs|Tasks|Logs|Tags|Holidays.
2. TaskListTab.xaml: month selector; grid (DataGrid/ItemsControl) columns code/project/type/PCT/PCA/internal-deadline/external-deadline/progress(bar+num, null="—")/logged/estimate(official??rough, whole-number)/chips. Chip order = system first (late before warning) then custom tags (HexToBrushConverter bg + icon + text). Expand row → tasks+status. Export-this-month button. Grid/Gantt toggle + collapse controls (Gantt body added W6).
3. ExportThisMonthCommand → ITaskListArchiveService.ExportMonthAsync(year,month).
4. Tests: TaskListViewModel.LoadAsync builds rows with correct logged hours + schedule state + estimate precedence + Done suppression (use in-memory/seeded db); DEFAULT excluded; month filter.
</action>
<verify>dotnet build + dotnet test green. Grid visuals = UAT. Automated <60s.</verify>
<done>Task List grid lists month backlogs with all columns + chips + expandable tasks; export button works; reloads on data change.</done>
</task>

## WAVE 6 — Gantt (depends W5 — SHARES TaskListTab/TaskListViewModel files, must follow)

<task id="W6" model="opus">
<read_first>
W5 TaskListViewModel + TaskListTab.xaml(+.cs); W2 IWorkingDayCalculator; spec §5.4 (Q3 rules); synthesis R5.
</read_first>
<action>
1. TaskListViewModel: build GanttModel — Axis = WorkingDaysBetween(min(start), max(deadline_internal), holidays); per backlog a GanttBar with StartDayIndex/SpanWorkingDays (start→deadline_internal), ExternalMarkerIndex (deadline_external on axis, nearest working day), HasStart flag, ScheduleState. No start_date → HasStart=false placeholder. Missing internal but has end_date → start→end neutral.
2. TaskListTab Gantt body: a Canvas inside the collapsible chart area; code-behind draws one row per bar (X=index*dayWidth using ActualWidth post-layout, R5), color by ScheduleState (late=red, warning=amber, else neutral theme brush), external-deadline marker, weekend/holiday gaps visually distinct, faint placeholder row for no-start bars. Redraw on Gantt/size change. Grid↔Gantt toggle + expand/collapse preserve SelectedMonth.
3. Tests: GanttModel geometry — axis excludes weekends/holidays; StartDayIndex/SpanWorkingDays correct for a known start/deadline; ExternalMarkerIndex placement; no-start bar flagged. (Canvas pixel drawing = UAT.)
</action>
<verify>dotnet build + dotnet test green (GanttModel geometry tests). Canvas rendering = UAT. Automated <60s.</verify>
<done>Grid↔Gantt toggle renders bars over a working-day axis colored by schedule state with external markers; geometry unit-tested; collapse works.</done>
</task>

## WAVE 7 — Sidebar restructure (depends W5 — needs TaskListTab)

<task id="W7" model="opus">
<read_first>
src/TimesheetApp/MainWindow.xaml (nav RadioButtons + content DockPanels + Timesheet sub-TabControl);
src/TimesheetApp/MainWindow.xaml.cs (OnSubTabChanged);
src/TimesheetApp/ViewModels/MainViewModel.cs (ActiveView, OnActiveViewChanged, ActivateTabAsync, ctors);
converters StringMatchConverter/StringMatchToVisibilityConverter; spec §5.1; synthesis A4/R2.
</read_first>
<action>
1. MainWindow.xaml: relabel "Timesheet" RadioButton → "Log Work" (keep ConverterParameter=timesheet). Add RadioButtons backlog, reports, tasklist (D2 order). Replace the disabled "Task List SOON" element with the enabled tasklist item. Split the Timesheet DockPanel: timesheet panel = Entry grid only; new sibling panels backlog (BacklogsTab), reports (ReportsTab), tasklist (TaskListTab) each Visibility-bound via StringMatchToVisibilityConverter. Delete the sub-tab TabControl + x:Name=SubTabs.
2. MainWindow.xaml.cs: delete OnSubTabChanged + its wiring.
3. MainViewModel: add public TaskListViewModel TaskList {get;} (injected both ctors). Extend OnActiveViewChanged: backlog→Backlogs.LoadAsync, reports→ActivateTabAsync(3), tasklist→TaskList.LoadAsync, dailyreport→DailyReport.LoadAsync, timesheet→ActivateTabAsync(0), users→2, settings→4. Reconcile BOTH stale doc comments (MainViewModel.cs ~:84-89 and ~:127). Keep ActivateTabAsync 0..4 map.
4. (DI injection of TaskListViewModel happens in W8; if needed earlier, ensure MainViewModel ctor accepts it.)
</action>
<verify>dotnet build + dotnet test green. NAV behavior = UAT (each item loads, no Timesheet/Standup/Reports regression — R2). Automated build <60s.</verify>
<done>Backlog/Task List/Reports are top-level; each loads; sub-tab control removed; no nav regression in build/tests.</done>
</task>

## WAVE 8 — DI + startup wiring + export (depends all)

<task id="W8" model="sonnet">
<read_first>
src/TimesheetApp/App.xaml.cs (DI block + OnStartup order incl standup backfill line); W1–W7 new types; spec §4.4,§5.1.
</read_first>
<action>
1. App.xaml.cs DI: register singletons ITagRepository/IPcaContactRepository/IHolidayRepository, IWorkingDayCalculator, IScheduleStateService, ITaskListArchiveService; transient TaskListViewModel. Pass TaskListViewModel into MainViewModel resolution.
2. OnStartup: after the standup BackfillMissingWeeksAsync call, add ITaskListArchiveService.BackfillMissingMonthsAsync() (same try/catch + ordering, before window shows / non-fatal).
3. Confirm SettingsViewModel resolves its new repo deps (registered above).
4. Smoke: dotnet build; run a headless-ish check is not feasible (WPF) — rely on build + full test suite.
</action>
<verify>dotnet build src/TimesheetApp.sln (0 errors); dotnet test src/TimesheetApp.sln (ALL green). Automated <60s.</verify>
<done>App composes with all new services/VM; startup backfills completed months; whole suite green. Ready for STEP 8 verification + UAT.</done>
</task>

---

## Wave dependency / overlap matrix
| Wave | Depends | Owns (no other wave touches) |
|---|---|---|
| W1 | — | DatabaseInitializer.cs, Entities.cs, ReadModels.cs, new repo files, BacklogRepository.cs, TimeLogRepository.cs |
| W2 | W1 | SmartInputService.cs, WorkingDayCalculator.*, ScheduleStateService.*, TaskListArchiveService.* |
| W3 | W1,W2 | SettingsViewModel.cs, SettingsTab.*, HolidayCalendarViewModel.cs, HexToBrushConverter, DataChangedMessage.cs |
| W4 | W1 | RequestEditorViewModel.cs, RequestsViewModel.cs, RequestsTab.xaml |
| W5 | W1,W2 | TaskListViewModel.cs, TaskListTab.* |
| W6 | W5 | TaskListViewModel.cs, TaskListTab.* (after W5) |
| W7 | W5 | MainWindow.*, MainViewModel.cs |
| W8 | all | App.xaml.cs |

Note: DataChangedMessage.cs edited in W3 only; MainViewModel ctor gains TaskListViewModel param in W7, registered in W8 — W7/W8 both touch MainViewModel/App but run sequentially (no parallel), acceptable.
