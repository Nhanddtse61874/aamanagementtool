<UserControl x:Class="TimesheetApp.Views.Tabs.UsersTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:beh="clr-namespace:TimesheetApp.Views.Behaviors">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- USR-02: add user input row -->
        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <Button Content="&#10133; Add user"
                    Command="{Binding AddUserCommand}"
                    DockPanel.Dock="Right" Padding="12,5" Margin="6,0,0,0"/>
            <Grid>
                <TextBox x:Name="NewUserBox" Text="{Binding NewUserName, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="New user name…" Foreground="{StaticResource Disabled}" Margin="10,0"
                           VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=NewUserBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </DockPanel>

        <!-- USR-01: user list with avatar + Active/Inactive badge -->
        <Border Grid.Row="1" Style="{StaticResource TableContainer}">
        <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionMode="Single"
                  BorderThickness="0" beh:RoundedClip.Radius="8">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Name" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Border Background="{Binding Name, Converter={StaticResource AvatarBrush}}" CornerRadius="12"
                                        Width="24" Height="24" Margin="0,0,9,0">
                                    <TextBlock Text="{Binding Name, Converter={StaticResource Initial}}"
                                               Foreground="White" FontWeight="Bold" FontSize="11"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Status" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Padding="9,2" HorizontalAlignment="Left"
                                    VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource BadgeGreenBg}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Background" Value="{StaticResource BadgeGrayBg}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock FontSize="12" FontWeight="SemiBold"
                                           Text="{Binding IsActive, Converter={StaticResource ActiveStatusConverter}}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource BadgeGreenFg}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Foreground" Value="{StaticResource BadgeGrayFg}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="" Width="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <!-- USR-03: deactivate — disabled for already-inactive users -->
                            <Button Content="Deactivate" Style="{StaticResource MiniGhostButton}"
                                    HorizontalAlignment="Right" Margin="0,0,6,0"
                                    IsEnabled="{Binding IsActive}"
                                    Command="{Binding DataContext.DeactivateCommand,
                                              RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding Id}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        </Border>
    </Grid>
</UserControl>
